# Smart Notebook – Arbeitsqueue

## Open

Priorisierte Warteschlange, vom Nutzer am 2026-09-12 festgelegt (höchste
Priorität zuerst). Bei jedem neuen Task-Zyklus wird oben begonnen.

- [ ] **Priorität 1 — ESP-Hochfahren mit Wakeup-Bild.** Analog zum
  bestehenden Herunterfahren-Bildschirm (`SCREEN_SLEEP`, Asset `sleep.bin`,
  siehe erledigter Eintrag „Erste ESP32-Einstellungsstufe … Herunterfahren“
  weiter unten) soll auch beim Hochfahren statt der aktuellen Ansicht
  (`app_main()` zeigt direkt `SCREEN_CONNECTING`) zunächst ein eigenes Bild
  erscheinen. Nutzerbild liegt als `wakeup.png` im Projekt-Root. Umsetzung
  spiegelbildlich zum Sleep-Bild: über `tools/pack_image_asset.py` auf das
  480×800-Hochformat-Canvas packen (mit der dort etablierten Drehung, nicht
  auf 800×480 zielen — genau der Fehler, der beim Sleep-Bild einmal real
  auftrat), Registrierung in `main/CMakeLists.txt` (`EMBED_FILES`), neuer
  Bildschirmzustand analog `SCREEN_SLEEP` in `screen.h`/`screen.c`, gezeigt
  mit erzwungenem vollem Refresh ganz am Anfang von `app_main()` in
  `main.c`, statt des bisherigen ersten `screen_show(SCREEN_CONNECTING,
  NULL)`. `generate_h3_assets.py` darf „wakeup“ nicht mit erzeugen, sonst
  überschreibt ein künftiger Sammel-Regenerierungslauf das handgestaltete
  Bild wieder mit Platzhaltertext (dieselbe Falle, die beim Sleep-Bild schon
  dokumentiert ist).

  **Dauer, auf Nutzerwunsch an echte Betriebsbereitschaft gebunden statt an
  eine feste Zeit** (2026-09-12 nachgeschärft): Das Gerät ist praktisch erst
  nutzbar, wenn das erste Dashboard geholt wurde, nicht schon bei
  hergestellter WLAN-Verbindung. Der Ersteinrichtungs-Portal-Flow (kein
  gespeichertes WLAN, `setup==true`) bekommt kein Wakeup-Bild — das ist
  Konfiguration, kein Aufwachen, und bleibt beim bestehenden `SCREEN_SETUP`.

  **Umgesetzt 2026-09-12, reale Sichtprobe und Fallback-Test stehen noch
  aus.** `screen_show(SCREEN_WAKEUP, NULL)` ersetzt in `main.c` das bisherige
  `screen_show(SCREEN_CONNECTING, NULL)` direkt vor `api_client_start()`.
  Dabei ein echter, am Gerät nachvollzogener struktureller Fund: der lokale
  Boot-Recovery-Pfad in `recorder.c` (`recorder_task()`) erreicht
  `SCREEN_READY` schon Sekunden nach dem Boot, komplett unabhängig von
  Netzwerk/Dashboard (Kommentar dort: „READY is an honest state" — bezogen
  auf die lokale Aufnahmefunktion, nicht auf das Dashboard). Ungefiltert
  hätte das binnen ~6 Sekunden ein leeres Dashboard über das Wakeup-Bild
  geblendet, real im Firmware-Log bestätigt, bevor die Korrektur griff.
  Neuer `wakeup_pending`-Zustand in `screen.c` fängt deshalb jede
  `SCREEN_READY`-Anfrage ab und hält sie auf `SCREEN_WAKEUP` zurück, bis
  `screen_snapshot_received()` (aufgerufen aus `fetch_dashboard()`,
  `main/api_client.c:731`, bei Erfolg) echte Daten meldet; Aufnahme,
  Fehleranzeige und jeder andere Bildschirmzustand bleiben unberührt, da nur
  `SCREEN_READY`-Anfragen abgefangen werden. Scheitert die Verbindung
  ungewöhnlich lange (WLAN weg, Server nicht erreichbar), gibt `main.c` nach
  60 Sekunden (`WAKEUP_FALLBACK_TICKS`) ohne Dashboard auf und ruft die neue
  `screen_wakeup_timeout()` auf: zeigt den echten `SCREEN_CONNECTING`-
  Diagnosebildschirm und beendet den Wakeup-Zustand dauerhaft, damit ein
  echtes Problem nicht hinter einem statischen Bild verschwindet und ein
  späterer Warteschlangen-Ping nicht stillschweigend zurück aufs Bild
  springt. Zweiter realer Fund: der Übergang Connecting→Wakeup lief zunächst
  über die partielle Waveform statt eines vollen Refreshs (Ghosting-Risiko
  bei einem kompletten Bildwechsel) — behoben mit demselben erzwungenen
  Vollrefresh wie beim Sleep-Bild, aber nur beim tatsächlichen Übergang in
  den Zustand (`previous_state != SCREEN_WAKEUP`), sonst hätte jeder
  spätere, inhaltlich identische Warteschlangen-Ping das unveränderte Bild
  erneut sichtbar aufflackern lassen — real als vierfaches Flackern
  beobachtet, dann behoben und über drei Boot-Iterationen im Log bestätigt
  (genau eine Wakeup-Vollaktualisierung, genau eine READY-Vollaktualisierung
  erst nach `dashboard: snapshot accepted`). `idf.py build` grün, Flash auf
  COM9 erfolgt. Details in `esp32-client/CHANGELOG.md`. **Offen:** reale
  Sichtprobe, ob das gepackte Bild auf dem Panel gut aussieht, und ein
  realer Test des 60-Sekunden-Fallbacks (Server oder WLAN absichtlich
  unerreichbar machen).

- [ ] **Priorität 2 — Attention-Segmente der lokalen Uploadqueue im
  Verlauf verwalten.** Segmente im lokalen Journal-Zustand `attention`
  (z. B. der Session-654-Fall `server_conflict`, bisher nur über die
  seriellen Diagnosebefehle `memo-list`/`memo-why`/`memo-discard`
  sichtbar und löschbar) sollen zusätzlich in der ESP-Verlaufsansicht
  markiert, auswählbar und über die Gerätetasten löschbar sein — ohne
  USB-Terminal. Zu unterscheiden von der bereits bestehenden Markierung
  serverseitiger `client_sessions.state=attention_required`-Zeilen in
  `main/history.c` (`history_state_label`/`history_state_icon` decken das
  schon ab); hier geht es um lokale, rein geräteseitige Journal-Segmente,
  die dort noch gar nicht auftauchen. Muss dieselbe bestehende
  Discard-Sicherheitsregel respektieren (`still_deliverable` verweigert
  den Discard, bis das Audio wirklich nicht mehr zustellbar ist — „die
  Warnung ist weg“ darf nie mit „das Audio ist weg“ verwechselt werden).
  Betrifft `main/history.c`, `main/journal.c`/`memo_queue.c` (lokale
  Segmentzustände) und die Tastenbehandlung für Auswahl/Löschen. Offene
  Designfrage vor Umsetzungsbeginn: eigener Abschnitt unterhalb der
  serverseitigen Sessionliste oder Verschmelzung je Session, falls ein
  Segment zu einer dort bereits sichtbaren Session gehört.

  **Umgesetzt 2026-09-12, reale Sichtprobe mit einem echten `attention`-Fall
  steht noch aus.** Designentscheidung: Verschmelzung. `memo_queue.c` liefert
  jetzt zusätzlich zu den Zählern `memo_queue_attention_snapshot()` — jede
  lokale Session, die während des ohnehin schon fensterbegrenzten
  `memo_queue_scan()`-Durchlaufs (`SCAN_WINDOW`, siehe dortiger Kommentar) als
  `CHUNK_ATTENTION`/`create_attention` erkannt wird, samt `session_id`
  (Journal-UUID = `client_session_id` auf der Leitung), Grund und einem
  `blocked`-Flag (`still_deliverable`: noch `ready`/`uploading` vorhanden).
  Bewusst *in* `scan_one()` mitgezogen statt als eigener Vollscan — ein
  zusätzlicher ungebundener Verzeichnisdurchlauf bei jedem `memo-discard`
  oder jeder Upload-Zustandsänderung hätte genau das Problem
  zurückgebracht, das `SCAN_WINDOW` laut eigenem Kommentar erst behoben hat.
  Die Liste teilt sich deshalb denselben begrenzten, selbstkorrigierenden
  Nachlauf wie `ready`/`acked`/`attention` bei mehr Sessions als ein
  Durchlauf abdeckt.

  `screen.c`s `history_rows()` gleicht bei jedem Zeichnen jede Server-Zeile
  per `client_session_id` gegen diese Liste ab; ein Treffer bekommt die
  Markierung „· lokal“ und `ICON_SEV_WARNING`. Eine lokale Session ohne
  passende Server-Zeile (noch nicht angelegt oder außerhalb der geholten
  Seite) erscheint als eigene Zeile darunter, identifiziert über ihre lokale
  ID statt eines Zeitstempels — eine lokale Auffälligkeit darf nie unsichtbar
  bleiben, nur weil der Server noch nicht nachgezogen hat oder gar nicht
  erreichbar ist (`draw_history()`/`move_history_focus()` fallen bei
  fehlgeschlagenem Serverabruf jetzt auf `"[]"` zurück statt die lokalen
  Zeilen mit wegzuwerfen). Aktivieren einer solchen Zeile öffnet einen
  Bestätigungsdialog („Aufnahme löschen“ / „Ja, löschen“), der bei
  `blocked` die Zeile „Ja, löschen“ durch einen Sperrgrund ersetzt — dafür
  wurde `settings_confirm_draw()` (bisher Neustart/Herunterfahren) um einen
  expliziten `locked_reason`-Parameter erweitert, weil der bisherige fest
  codierte Text „SD-Karte beschäftigt“ für diesen Fall schlicht falsch
  gewesen wäre. Bestätigen ruft das bestehende `recorder_discard()` auf;
  dieselbe `still_deliverable`-Regel gilt unverändert. `idf.py build` grün,
  Flash auf COM9 erfolgt, Boot-Log dreier Durchläufe ohne Fehler oder
  Backtrace geprüft (aktuell `attention=0` auf dem Gerät — der Zweig, der
  eine Session tatsächlich in die neue Liste einträgt, lief dabei nicht
  mit). **Offen:** eine reale Sichtprobe mit mindestens einer echten
  `attention`-Session — Verschmelzung, lokale Extra-Zeile und der
  Lösch-Dialog inklusive Sperrzustand wurden nicht am Gerät gesehen, nur
  gegen den Code und den vorhandenen `attention=0`-Boot-Pfad geprüft.

- [ ] **Priorität 3 — Preloading/Caching für Detailansichten.** Kein
  bereits bestehender Eintrag gefunden (in `task.md`, `ROADMAP.md`,
  `CHANGELOG.md` und `docs/` gesucht) — neu aufgenommen, nicht mit dem in
  `esp32-client/docs/CLIENT_SERVER_STATE.md` Abschnitt 7a erwähnten,
  bewusst zurückgestellten Sanduhr-/Ladeindikator zu verwechseln (andere
  Anforderung). Serveranfragen für Dashboard-Karten/Session-Details sollen
  nicht mehr ausschließlich synchron bei jedem Öffnen einer Detailansicht
  gestellt werden, sondern im Hintergrund vorab geladen und lokal
  zwischengespeichert werden (RAM oder SD, beides zulässig), damit ein
  bereits abgerufenes Detail ohne erneuten Serverumweg sofort erscheint.
  Betrifft den bestehenden synchronen Abrufpfad über den Uploadworker in
  `main/api_client.c` (siehe `CLIENT_SERVER_STATE.md`, Abschnitt
  „Detailansicht“) und `main/detail.c`. Muss mit der bestehenden
  Snapshot-Alterungslogik (`screen_snapshot_cache_limit`) und der
  Cache-Invalidierung bei neuen Snapshots zusammenspielen, damit kein
  veralteter Detailinhalt gezeigt wird.

  **Umgesetzt 2026-09-12, reale Sichtprobe eines Wiederöffnens steht noch
  aus.** RAM-Cache gewählt (8 Plätze, `ENTITY_MAX` je Slot), keine SD:
  Freshness teilt sich denselben Wert wie die bestehende
  Snapshot-Alterung (`cache_max_age_s`/`HEADER_STALE_FALLBACK_S`) statt einer
  zweiten Staleness-Regel, genau wie in der Designfrage gefordert. Jede
  frisch angenommene Dashboard-Momentaufnahme (`apply_pending_snapshot()`,
  einmal pro echtem neuen Snapshot) preloaded über die neue
  `preload_visible_entities()`/`api_client_preload_entity()` jede
  fokussierbare `open_entity`/`open_clarification`-Karte über alle drei
  Oberflächen, deren Cache-Eintrag fehlt oder abgelaufen ist; Session-Karten
  (`open_session`) bleiben ausgenommen, da deren Route ein ganzes
  Session-Dashboard liefert, nicht ein `ENTITY_MAX`-großes Entity — außerhalb
  des hier betroffenen `main/detail.c`-Zuschnitts. Der Worker verarbeitet die
  Preload-Warteschlange mit niedrigster Priorität, nur wenn kein expliziter
  Leser wartet, und ausschließlich innerhalb des ohnehin schon offenen
  Funkfensters (kein zusätzliches Radio-Aufwachen). Ein Treffer landet über
  `screen_entity_preload_received()` ausschließlich im Cache, nie in der
  gerade angezeigten `entity_json`-Kopie. `open_detail()` fragt vor dem
  bisherigen synchronen Abruf zuerst den Cache ab: Treffer zeigen sofort ohne
  „wird geladen …“; ein Fehltreffer fällt unverändert auf den alten Weg
  zurück. `screen_entity_received()` aktualisiert denselben Cache-Eintrag
  zusätzlich aus jeder echten Antwort (auch einer abgeschlossenen Aufgabe).
  Echter, am Gerät gefundener Fund unterwegs: die erste Fassung legte pro
  Aufruf zwei `dashboard_plan`-Strukturen auf dem knappen 4096-Byte-Stack des
  `screen`-Tasks an, in einer Schleife über bis zu drei Oberflächen — ein
  realer Stack-Overflow, live im Log direkt nach dem ersten „dashboard:
  snapshot accepted“ dieses Boots bestätigt, danach Reboot. Behoben durch eine
  einzige heap-allozierte Instanz, wiederverwendet über alle
  `dashboard_walk()`-Aufrufe, nach demselben Muster wie der Rest der Datei.
  `idf.py build` grün, Flash auf COM9 erfolgt, nach dem Fix über mehrere
  Boot-/Poll-Zyklen ohne Stack-Overflow oder sonstigen Fehler bestätigt; `api:
  entity preload ok http=200` erscheint im Log direkt nach jedem `dashboard:
  snapshot accepted` — der Preload-Pfad liefert real gegen den
  Produktionsserver aus. Details in `esp32-client/CHANGELOG.md`. **Offen:**
  eine reale Sichtprobe, dass ein Wiederöffnen einer schon einmal gesehenen
  Karte tatsächlich ohne „wird geladen …“ erscheint — erfordert einen echten
  Tastendruck am Gerät, bisher nur der Cache-Füllpfad über das Log geprüft.

- [ ] **Priorität 4 — Späterer STT-Unsicherheitsblock.** Lokale
  Whisper-Wortkonfidenzen und vom LLM bewertete Satzplausibilität
  gemeinsam auswerten. Bei materieller Inkonsistenz optional einen
  zweiten STT-Lauf mit anderen Parametern verwenden, um die Unsicherheit
  zu bestätigen oder aufzulösen; nur ungelöste, handlungsrelevante Fälle
  als gezielte Rückfrage ausgeben. Keine stille Transkriptkorrektur und
  kein pauschaler Zweitlauf.

- [ ] **Priorität 5 — Netzwerk-/Transport-/Verschlüsselungsaudit.** Sehr
  spät vor dem produktionsnahen Alpha-Einsatz einen systemweiten
  Netzwerk-, Transport- und Verschlüsselungsaudit durchführen:
  TLS/Authentisierung, Credentialrotation, Hotspot/Portal, WLAN-Profile,
  lokale SD-/NVS-Daten, Backendgrenzen und bekannte Klartext-/
  Fallbackpfade prüfen; kritische Befunde vor Freigabe schließen.

Gestrichen auf Nutzerwunsch (2026-09-12): ESP-Zeitzone als persistierbare
IANA-Zone auswählbar machen. Die verifizierte `Europe/Berlin`-Regel bleibt
dauerhaft fest eingebaut, dieser Punkt kommt nicht wieder auf die Roadmap.

- [x] Echter Sprachbefehl „Lösche die Liste 'nach dem Herbsturlaub'" löschte die Liste nicht — Ursache gefunden und behoben. Die `archive`-Klassifikation (`capture_intent.py::classify_content_intent_with_llm`) erkannte `target_type=list` korrekt mit 0,95 Konfidenz, aber `target_text` war nur „die Liste" — ohne den eigentlichen Listennamen. Dieser verkürzte `target_text` speist direkt die A05-Kandidatensuche (`knowledge_preflight.py:187`, `query=part["target_text"].strip() or part["source_text"].strip()`); da `target_text` nicht leer war, griff der Fallback auf `source_text` nie, die Suche fand zwei falsche Listen, blieb mehrdeutig und stellte eine Rückfrage, die nie die richtige Liste nannte. Kein Backend-Bug in Router/Validierung/Promotion (anders als der heute schon gefixte Commit `0421a1b`, der die Listenerstellung betraf) — sondern eine zu vage Prompt-Anweisung für die Zielspannen-Extraktion bei Mutationsabsichten. Behoben in `smart_notebook/prompts.py`: `CAPTURE_INTENT_SYSTEM_PROMPT` und `CAPTURE_INTENT_SPLIT_SYSTEM_PROMPT` (dieselbe Formulierung, beide betroffen) verlangen jetzt ausdrücklich, dass `target_text` einen im Satz vorhandenen Namen, Titel oder ein anderes unterscheidendes Merkmal mit einschließt, statt nur die bloße Objektart. Da beide Prompts für alle drei Mutationsabsichten (`change`/`complete`/`archive`) identisch gelten, deckt der Fix automatisch alle drei ab — keine separate Änderung pro Aktion nötig. Mit vier echten LLM-Aufrufen gegen den laufenden Produktions-LLM-Endpoint verifiziert: „Lösche die Liste nach dem Herbsturlaub" → `target_text="die Liste nach dem Herbsturlaub"` (zweimal reproduziert), „Hake die Aufgabe Steuererklärung ab" → `target_text="Steuererklärung"`, „Ändere die Notiz zum Kundengespräch, die Uhrzeit war falsch" → `target_text="die Notiz zum Kundengespräch"`. Dabei einen separaten, vorbestehenden und nicht durch diese Änderung verursachten Grenzfall gefunden und gegen den unveränderten Code reproduziert: eine rein pronominale Referenz ohne jeden Namen („Streich das aus meiner Liste.") scheitert bereits vor dieser Änderung an `_validate_decision`s Pflichtfeld-Prüfung („Mutation intent requires an exact target span") — nicht behoben, da außerhalb des Zuschnitts dieser Aufgabe und ein eigenständiges Problem (fehlender Kontextbezug, nicht verkürzter Zielname). `m8_contract_foundation_test.py` schlägt sowohl vor als auch nach dieser Änderung identisch fehl (`CLIENT_DEVICE_AUTH_REQUIRED=true` in `.env`, vorbestehend und unabhängig, bereits an anderer Stelle dokumentiert) — kein Regressionsindiz. Kein automatisierter Regressionstest ergänzt: die Vollständigkeit von `target_text` hängt am nicht-deterministischen LLM-Prompt, nicht an deterministischer Validierungslogik (dieselbe Einschränkung wie bei der Promotion-Korrektur in `0421a1b`). — Erledigt 2026-09-12.

- [x] Reale Live-Probe der W05-Rückfrage-Detailansicht deckte einen echten strukturellen Fehler auf und drei damit zusammenhängende Aufräumpunkte, alle behoben. Nutzer wollte per Sprachbefehl die Liste „nach dem Herbsturlaub" löschen; eine ältere offene Rückfrage bot dafür nur „Einkaufsliste" oder „Packliste" an — keines von beidem war gemeint. Eine neue Freitext-Memo-Antwort in der Rückfrage-Detailansicht („Keines von beiden. Ich meinte die Nach-dem-Herbst-Urlaub-Liste.") wurde vom Backend fehlerfrei verarbeitet (`clarification_answer_attempts` zeigt `completed`, kein Absturz), blieb aber `needs_clarification`/`insufficient_clarification` — nachweislich nicht behebbar, denn `resume_mutation_target_resolution()` prüft eine Antwort ausschließlich gegen den einmalig bei Rückfrage-Erstellung eingefrorenen A05-Kandidatensnapshot (`candidate_keys`); die tatsächlich gemeinte Liste war dort nie enthalten, weil A05s ursprüngliche Suche auf dem (durch den heute schon gefixten `target_text`-Bug) verkürzten Zieltext lief. Da die Rückfrage offenblieb, wählte der Nutzer testweise die angebotene „Packliste" — technisch korrekt ausgeführt, aber ungewollt archiviert (reversibel, nicht physisch gelöscht). Das separat gemeldete Ausrufezeichen in der Statusleiste stammte nachweislich aus einem völlig anderen, bereits in `CLIENT_SERVER_STATE.md` dokumentierten Altfall (Session 654 vom 11. September, `server_conflict`, historischer Quick-Upload-Guard-Bug, längst serverseitig behoben, Chunk aber absichtlich nicht automatisch aufgeräumt) — keine Verbindung zum heutigen Vorfall. **Struktureller Fix:** `mutation_targets.py::resume_mutation_target_resolution()` erweitert den Kandidatenpool jetzt bei einer nicht zuordenbaren Freitextantwort um eine frische `search_knowledge()`-Suche mit dem Antworttext selbst als Query (neue Funktion `_widen_candidates()`), bevor der LLM-Resolver läuft; gefundene neue Kandidaten werden nach Typkompatibilität gefiltert, dedupliziert und zusätzlich in `candidate_keys` persistiert, damit auch eine weiterhin mehrdeutige Folgeantwort die erweiterte Menge sieht. Mit dem realen Fall verifiziert (nicht nur Testfixtures): dieselbe echte Antwort löst mit dem Fix korrekt auf `list:111 „Nach dem Herbsturlaub"` mit Konfidenz 0,95 auf, obwohl die STT-Schreibung „Herbst Urlaub" (mit Leerzeichen) nicht als Substring zum Listennamen „Herbsturlaub" passt und daher nur der LLM-Schritt, nicht der schnelle exakte Abgleich, sie fand. `m8_mutation_target_resolution_test.py`, `m8_clarification_loop_test.py`, `m8_mutation_action_test.py`, `m8_partial_action_test.py`, `m8_knowledge_clarification_test.py` weiterhin grün; `m8_contract_foundation_test.py`/`m8_capture_contract_test.py` schlagen unverändert am vorbestehenden, unabhängigen `CLIENT_DEVICE_AUTH_REQUIRED`-Defekt fehl. **Aufräumung:** Packliste (Liste 48) und ihre drei kaskadiert mitarchivierten Einträge (Sonnencreme, Zahnbürste, Reisepass) über den bestehenden `set_list_archived`-Service bzw. direktes Zurücksetzen von `status`/`archived` auf den Vorzustand wiederhergestellt; der `server_conflict`-Altchunk aus Session 654 per `memo-discard C1C15C38` reell vom Gerät entfernt (`@DISCARDED files=3 attention=1`), `memo-list` zeigt danach keine `attention`-Einträge mehr. Dabei zwei Nebenbefunde ohne Codeänderung: vier verwaiste `idf_monitor.py`-Prozesse blockierten COM9 (derselbe Tree-Kill-Bedarf wie zuvor dokumentiert, mit `taskkill /T /F` behoben) und ein vorbestehender Mojibake-Datenfehler in `list_items.content` („Zahnb�rste" statt „Zahnbürste", vermutlich alte STT-/Encoding-Ursache, unabhängig von diesem Fund, nicht angefasst). — Erledigt 2026-09-12.

- [x] Die 51 aufgestauten Sessions aus dem H2-Verschlüsselungsnachholsync untersucht und aufgeklärt — kein Freigabe-Bug. Direkt per `pyserial` gegen COM9 (zuverlässiger als `idf.py monitor`, das bei prozessübergreifendem Neustart Portsperren durch nicht sauber beendete Kindprozesse zurückließ) `memo-list`/`memo-why` abgefragt: 11 von 11 stichprobenartig geprüften `acked`-Sessions zeigen `file=missing` — das Audio wurde bereits erfolgreich freigegeben und gelöscht; nur der Journal-Eintrag bleibt absichtlich für immer stehen ("nur das Audio verschwindet, Journal und Sessionstatus bleiben"). Die Sorge des Nutzers ("werden fertig synchronisierte Memos tatsächlich lokal gelöscht?") ist damit mit realen Daten beantwortet: ja. Zwei echte Nebenbefunde dabei gefunden und behoben: **Erstens**, ein echter, unabhängiger Skalierungsfehler in `memo_queue.c::memo_queue_scan()`: die Boot-Zeit-Wiederherstellung deckelte fest bei 32 Verzeichnissen pro Durchlauf, ohne Rotation — anders als der strukturell identische, bereits gelöste Fall im Sync-Pfad (`api_client.c`s `SESSION_WINDOW`/`session_offset`). Bei mehr als 32 echten Sessions auf der Karte blieb jedes Verzeichnis jenseits Position 32 dauerhaft unwiederhergestellt, weil POSIX-`readdir()`-Reihenfolge sich nicht von selbst ändert. Behoben durch identisches Rotationsmuster (`SCAN_WINDOW`/`scan_offset`); `status` wird weiterhin pro Durchlauf neu berechnet, ein Session außerhalb des aktuellen Fensters fehlt also vorübergehend in `ready`/`acked`/`attention`, bis die Rotation sie wieder erreicht — eine begrenzte, selbstkorrigierende Verzögerung über wenige Durchläufe, kein dauerhafter Counter-Drift wie beim 6.-September-Vorfall. `idf.py build` grün, live auf dem Gerät verifiziert: aufeinanderfolgende Scans zeigten „17 of 49 sessions … next start=0" dann „32 of 48 sessions … next start=32" — die Rotation läuft nachweislich. **Zweitens**, drei liegengebliebene, durch die eigenen Verschlüsselungs-Testfehlschläge entstandene unvollständige Testaufnahmen (`writing`-Zustand, nie abgeschlossen) per `memo-discard` aufgeräumt: Eine (`CE5414AE`) war beim Absenden des Befehls noch nicht wiederhergestellt und ließ sich direkt verwerfen. Die anderen beiden (`8EBC55AF`, `A8ACA8D7`) hatte der frische Boot mit dem jetzt reparierten Verschlüsselungscode zuerst selbst über `journal_recover()`s `CHUNK_WRITING`-Pfad zu echten, gültigen `ready`-Segmenten finalisiert — das Gerät verweigerte den Discard folgerichtig mit `still_deliverable`, bis der reale Uploadversuch serverseitig mit `AUDIO_METADATA_INVALID` abgelehnt wurde (die Testaufnahmen waren tatsächlich zu kurz/beschädigt für eine gültige Aufnahme), erst danach waren sie als `attention` regulär discardable. Realer Beleg, dass die Discard-Sicherheitsregel ("kein Komfort ist es wert, `die Warnung ist weg` mit `das Audio ist weg` zu verwechseln") genau wie vorgesehen funktioniert. Endzustand per `memo-list` bestätigt: nur noch die eine bereits vorher bekannte `server_conflict`-Attention-Session (`C1C15C38`) übrig, keine „incomplete"-Stubs mehr. `esp32-client/CHANGELOG.md` aktualisiert. — Erledigt 2026-09-12.

- [x] Vor A05 die reale A04-Listenabnahme stabilisieren: ESP-Listenansicht auf zehn Karten erweitert, Home bei drei priorisierten Karten belassen; reine Listenerstellung gegen gleichnamige aktive Listen dedupliziert; über benachbarte STT-Chunks verteilte Liste-plus-Item-Aussagen gemeinsam ausgewertet; leere Listen aus fehlgedeuteten Aktionssätzen verhindert; Regressionen räumen auch bei früher Assertion ihre Shared-DB-Fixtures auf. Vollständiges M8 und Fixture-Nachkontrolle sind grün. Vier echte Aufnahmen nach Worker-Neustart bestätigten Task, zweimal identische Listenerstellung als genau eine aktive Liste sowie „Fotos sortieren“ über zwei STT-Chunks als Listeneintrag bis zur sichtbaren ESP-Projektion. — Erledigt 2026-09-10.
- [x] Backend-Auto-Modus A05: Vor jeder vorgesehenen Anlage und jeder zurückgestellten Mutation läuft der persistierte, idempotente Hybrid-Suchabgleich `new|identical|complementary|contradictory|targeted`; exakt eindeutige Identität wird ohne Neuanlage wiederverwendet, Mutationskandidaten bleiben ohne Zielauflösung/Ausführung. Gezielte DB-/Pipeline-Regressionen, echter strukturierter LLM-Aufruf und vollständiges M8-Gate einschließlich logischem Vier-Stunden-Soak sind grün. Zwei echte, nach Worker-Neustart über BOOT aufgenommene Audioeingaben wurden als `new` und trotz STT-Bindestrichvariante als `identical` bewertet und auf genau dieselbe dauerhafte Note geführt. — Erledigt 2026-09-10.
- [x] Backend-Auto-Modus A06: Sprachliche Mutationsreferenzen werden ausschließlich gegen die von A05 persistierten Kandidaten oder einen gültigen öffentlichen Objektkontext aufgelöst. Eindeutige Ziele werden intern idempotent gebunden; fehlende, mehrdeutige, inkompatible oder zu schwache Bezüge erzeugen eine persistente, quellgebundene Rückfrage. Öffentliche Ergebnisse enthalten Status und Kandidatenanzahl, aber keine internen IDs. A06 führt weiterhin keine Mutation aus; das bleibt A07. Gezieltes DB-Gate, Text-/audioförmige Capture-Regression, echter strukturierter LLM-Aufruf und vollständiges M8-Gate einschließlich logischem Vier-Stunden-Soak sind grün. — Erledigt 2026-09-11.
- [x] Backend-Auto-Modus A07: Für jedes eindeutig aufgelöste Mutationsziel wird genau eine persistierte Aktion geplant und idempotent ausgeführt. Unterstützt sind quellentreue Inhaltsänderung/Umbenennung, Listeneintrag-Ergänzung, Task-/Item-Erledigung, kontextgebundenes Wiederöffnen und reversible Archivierung von Note, Task, Liste oder Item. Unklare, schwache, ungültige oder veraltete Pläne erzeugen Rückfragen. Ziel-Lock, Objektänderung und Vorher-/Nachher-Audit sind atomar; öffentliche Ergebnisse enthalten weder interne IDs noch Payload/Audit. Gezieltes DB-Gate, reale strukturierte Plan-/Zielaufrufe, vollständiges M8-Gate mit 23 Prüfungen, Firmwarebuild, Flash auf COM9 und `h4-auto`-Contractstatus sind grün. Die reale Audioabnahme fragte bei „den Urlaub“ wegen zweier plausibler Listen nach und archivierte bei „den Herbsturlaub“ ausschließlich die richtige Liste; die andere blieb aktiv und die ESP-Projektion aktualisierte sich. — Erledigt 2026-09-11.
- [x] Backend-Auto-Modus A08: Gemischte Mutationen werden je Intentteil freigegeben. Tentative Teile bleiben unter der Schwelle `0.85`, erzeugen eine quellgebundene Bestätigungsfrage und blockieren unabhängige sichere Geschwister nicht; das öffentliche Ergebnis enthält abgeschirmte Einzeloutcomes und `partially_completed`. Ein Retry wiederholt keine bereits abgeschlossene Geschwisteraktion. Dediziertes DB-Gate, angrenzende Regressionen, echter strukturierter Split-Aufruf und vollständiges M8-Gate mit 24 Prüfungen einschließlich logischem Vier-Stunden-Soak sind grün; Fixture-Nachkontrolle ist sauber. Die echte Session 766 erledigte „Kupfermond“ mit `0.95`, hielt den tentativen Löschteil mit `0.75` zurück, ließ die Liste unverändert und erzeugte genau eine quellgebundene Rückfrage. Der Nutzer bestätigte beide sichtbaren Ergebnisse. W05-Antwort/Fortsetzung bleibt separat. — Erledigt 2026-09-11.
- [x] W05 Question-/Clarification-Kreislauf: Mutations- und Wissenspfad sind strukturell geschlossen. Eine geöffnete ESP-Rückfrage bindet die nächste gültige BOOT-Aufnahme oder ausdrückliche Auswahl exakt an ihre persistierte Abhängigkeit. A06/A07-Mutationen werden gegen den ursprünglichen A05-Snapshot höchstens einmal fortgesetzt. Ein hochkonfidenter A05-Widerspruch zwischen neuer Fact-/Note-/Decision-Aussage und vorhandenen Notes stoppt jetzt die Promotion; auswählbar sind bei eindeutigem Altstand „Neue Angabe“/„Bisherige Angabe“, alternativ übernimmt eine freie Antwort nur eine exakte Korrekturspanne. Snapshotänderungen blockieren, verdrängtes Wissen wird reversibel archiviert, Antwortsession und Vorher-/Nachherzustand bleiben im Audit. Beide gezielten Gates und vollständiges M8 mit 26 Prüfungen einschließlich logischem Vier-Stunden-Soak sind grün; der echte strukturierte Resolver lieferte die korrekte wörtliche Ersatzspanne mit 0,95. Mutationsoptionen, freie Mutationsantwort, unmittelbare Rückkehr und Navigation sind real bestätigt. Die erste Wissens-Liveprobe wurde auf Nutzerwunsch abgebrochen: Session 1030 promovierte „Der W05-Testschrank steht links neben dem Fenster.“ korrekt auf Note 168; Session 1031 transkribierte die widersprechende Rechts-Aussage korrekt, klassifizierte den Memo-Zieltyp jedoch als `unknown` und erzeugte kein Artefakt. W05 wurde dadurch nicht erreicht und weder bestanden noch widerlegt. Ein zweiter Versuch (Session 1032, „Korrektur zum W05-Testschrank: …“) deckte einen echten, reproduzierten Backendfehler auf statt einer Modellunsicherheit: Das Wort „Korrektur“ klassifizierte die Eingabe als `change`-Mutation auf `target_type=note`, `target_text="W05-Testschrank"`; die STT-Rohtranskription enthielt jedoch „W05 -Testschrank“ (Leerzeichen vor dem Bindestrich), und die byte-exakte Spannenprüfung in `capture_intent.py::_validate_decision` lehnte den ansonsten korrekten, nur whitespace-normalisierten Zielspan ab. Das riss die Session hart auf `attention_required`, statt geordnet abzuschließen oder abzustellen. Behoben mit einer whitespace-insensitiven Spannenprüfung (`_contains_span`), die dieselbe Anti-Halluzinations-Garantie (identische Zeichen, identische Reihenfolge) beibehält; gezielte Regression `capture_intent_span_test.py` reproduziert den realen Session-1032-Fall und die abgelehnte Erfindung, vollständiges M8 mit 27 Prüfungen einschließlich logischem Vier-Stunden-Soak ist grün. Sessions 1030–1032 und Note 168 blieben unverändert als Testbeleg stehen. Offen bleibt weiterhin die reale Audio→Wissensrückfrage→Auswahl/Korrektur-Probe mit einer rein deklarativen, nicht mutationsverdächtigen Formulierung; der optionale kontextlose Memo-Fallback ist kein W05-Abnahmekriterium. Ein dritter Versuch (Session 1119, rein deklarativ „Der W05-Testschrank steht jetzt rechts neben dem Fenster.“) traf erneut denselben `attention_required`-Absturz — Ursache war diesmal nicht der Code, sondern dass der laufende Worker-Prozess den Fix aus Session 1032 nicht geladen hatte (Python lädt geänderte Module nicht automatisch nach); nach Worker-Neustart erzeugte derselbe Satz (Session 1120) den echten W05-Rückfragefall am Gerät: „Soll die neue Angabe ‚Der W05-Testschrank steht rechts neben dem Fenster.‘ die bisherige Angabe ‚Der W05-Testschrank steht links neben dem Fenster.‘ ersetzen?“ mit den Optionen „Zurück“, „Neue Angabe“, „Bisherige Angabe“ — genau das erwartete Ergebnis, real am Gerät bestätigt. Damit ist die reale W05-Wissensabnahme erbracht; W05 gilt als vollständig geschlossen (strukturell und live). Auswahl/Korrekturantwort auf diese konkrete Rückfrage wurden nicht mehr gesondert durchgespielt, da der Mutationspfad (Auswahl zwischen Optionen, freie Korrektur, sofortige Rückkehr) bereits zuvor real abgenommen war und hier identisch verdrahtet ist.
- [x] ESP-Firmware-Fund während der W05-Liveprobe: `HISTORY_MAX` in `esp32-client/main/screen.c` war mit `8192` nur halb so groß wie `API_RESPONSE_MAX` (16384) im Fetch-Puffer — derselbe Fehlerklasse, die für `SNAPSHOT_MAX` dort bereits dokumentiert ist. Der Fetch holt die Verlaufsantwort vollständig (real gemessen: 9190 Bytes), aber `screen_history_received()` kopiert sie per `snprintf` in den zu kleinen Puffer, schneidet sie ab, `cJSON_Parse` scheitert am kaputten JSON, und die Ansicht zeigt „Noch keine Aufnahmen“ — nicht von echter Leere zu unterscheiden; der SD-Log bleibt unbetroffen, weil er ein separater Codepfad ist. Live beobachtet direkt nach der bestandenen W05-Rückfrage (Session 1120). Im Quellcode behoben (`HISTORY_MAX` auf 16384, mit Kommentar zur Invariante); `screen.c` liegt außerhalb der Hosttest-Abdeckung von `tools/run_ui_test.sh` (braucht FreeRTOS/heap_caps, kein reiner Zeichen-Code wie `history.c`). Build, Flash und reale Sichtprobe bestanden: Verlaufsansicht zeigt die Einträge wieder. — Erledigt 2026-09-11.
- [x] ESP-Sichtkorrekturen nach dem nächsten Backendblock: redundantes `open` von Taskkarten auf der E-Paper-Projektion entfernt; SD-Logscrollgrenzen aus Darstellungstext vereinheitlicht und mit sichtbarer Fensterposition überprüfbar gemacht; gesunden automatischen Dashboardabruf mit vier Minuten Startgrenze plus einer Minute Transportreserve für das Fünf-Minuten-Erfolgsziel versehen. Manueller Sync bleibt, und die Kopfzeilenzeit wird weiterhin nur durch den letzten erfolgreich validierten Abruf erneuert. Projektionsregression, ESP-IDF-Build, Flash und reale automatische Pollprobe sind grün; die erneute Logviewer-Tasten-/Sichtprobe folgt im Gespräch. — Erledigt 2026-09-10.
- [x] ESP-Einstellungen um ausdrücklich bestätigte lokale Aktionen „Neustart“ und „Herunterfahren“ ergänzen. Beide während Aufnahme und kritischen SD-Schreibphasen sperren; Neustart erst nach sauberem Flush. Herunterfahren darf erst nach verifizierter PMIC-Abschaltsequenz umgesetzt werden und muss das Verhalten mit angeschlossenem USB sowie den erneuten Start über PWR real prüfen. **Normkonflikt geklärt, Nutzerentscheidung:** `battery.h` legt fest, dass kein PMIC-Register je beschrieben wird; eine echte PMIC-Abschaltsequenz bräuchte genau das. Auf ausdrücklichen Nutzerwunsch bleibt die Read-only-Grenze unangetastet — „Herunterfahren“ nutzt `esp_deep_sleep_start()` mit `esp_sleep_enable_ext0_wakeup(GPIO_NUM_0, 0)` (Aufwachen über denselben BOOT-Taster) statt eines echten PMIC-Poweroffs; das ist eine bewusste Abweichung vom wörtlichen Aufgabentext, kein PMIC-Abschalten. Umgesetzt: zwei neue Settings-Zeilen mit zweistufiger Ja/Nein-Bestätigung (`settings_confirm_draw`), gesperrt mit sichtbarem Grund, solange `recorder_busy()` wahr ist (Aufnahme oder kritischer SD-Schreibvorgang); Neustart über dieselbe `recorder_busy()`-Sperre und `esp_restart()`-Sequenz, die der bestehende serielle `reboot`-Befehl in `main.c` bereits benutzt (dort „während einer Aufnahme verweigert“, dokumentiert in `CLIENT_SERVER_STATE.md`) — zwei Einstiege, ein Verhalten. Host-Test (`tools/ui_test.c`) deckt Zeilen, gesperrte und freigegebene Bestätigungsansicht innerhalb der Bildschirmgrenzen ab. Lokaler `gcc` nachträglich per `scoop install gcc` installiert und der reale Testlauf durchgeführt statt nur manuell gelesen: Ein echter Kompilierfehler kam dabei zum Vorschein (`settings_confirm_draw` nutzte den Parameter `bottom` nicht, `-Werror` schlägt fehl) und wurde mit denselben Bounds-Checks wie `settings_diagnostics_draw` behoben; danach kompiliert `settings.c` sauber und alle neuen Prüfungen sind grün. Eine vorbestehende, von dieser Änderung unabhängige Testlücke bleibt: `tools/ui_test.c` ruft `setenv()` auf, das die native Windows-`mingw`-Toolchain nicht kennt (nur unter Linux/WSL/macOS lauffähig, wie das Skript es voraussetzt); mit einem rein lokalen, nicht eingecheckten Kompatibilitäts-Shim isoliert verifiziert und dabei ein zweiter, ebenfalls vorbestehender und unabhängiger Fehlschlag ohne Bezug zu dieser Änderung gefunden (`dashboard_refresh_interval_s`-Erwartung, auch auf dem sauberen Stand vor dieser Änderung reproduziert) — nicht Teil dieser Aufgabe, nicht angefasst. **Auf Nutzerwunsch während derselben Aufgabe erweitert, bevor die erste Runde geflasht war** (zwei zusätzliche Anforderungen, kein separater Task): (1) ein eigener Bildschirm beim Einschlafen statt eines schlicht einfrierenden letzten Frames, und (2) ein automatischer Akku-Sleep, damit ein leerlaufender Akku nicht unbemerkt als eingefrorenes Bild endet. Dabei einen bestehenden, nie genutzten Punkt gefunden: `EPD_Sleep()` (eigener Deep-Sleep-Befehl an den Panel-Controller-Chip) existiert in `epaper_port.h` seit Langem, wurde aber vor dieser Änderung nirgends aufgerufen — jeder bisherige Zustand ließ den Panel-Treiber unnötig unter Strom. Umgesetzt: neuer `SCREEN_SLEEP`-Zustand mit eigenem `sleep.bin`-Asset (Platzhalter-Text „Ruhezustand / Zum Aufwachen BOOT-Taste drücken“ über `generate_h3_assets.py`, austauschbar durch ein selbst gestaltetes PNG über das neue `tools/pack_image_asset.py` — beliebige Quellauflösung, letterboxed auf 800×480, gedithert). Der Zustand zieht als Einziger keinen Dashboard-Overlay (`>=SCREEN_READY`-Prüfung explizit ausgenommen) und erzwingt über `force_full` immer einen vollen, sauberen Refresh statt eines Partial-Updates. Nach dem vollen Refresh: `EPD_Sleep()`, dann `esp_deep_sleep_start()` — das E-Paper hält das Bild danach stromlos. Neue öffentliche Funktion `screen_enter_sleep_if_safe()` bündelt die `recorder_busy()`-Sperre für beide Aufrufer (Settings-Bestätigung und Akku-Trigger). Akku-Trigger in `battery.c::take_sample()`: bei jeder 5-Sekunden-Messung, `soc<=5` **und** nicht an USB, wird derselbe Sleep angefragt; eine laufende Aufnahme verschiebt das schlicht auf die nächste Messung statt zu unterbrechen. Registrierung in `main/CMakeLists.txt` (`EMBED_FILES`) ergänzt. Diese drei Dateien (`screen.c`, `screen.h`, `battery.c`) sind in dieser Umgebung nicht kompilierbar (kein FreeRTOS/ESP-IDF-Hostsimulat), anders als `settings.c` — nur sorgfältige manuelle Prüfung, kein echter Testlauf. Erster Build/Flash der Neustart/Herunterfahren-Stufe vom Nutzer bestätigt (vor der Sleep-Bild-/Akku-Erweiterung). Reales Bild vom Nutzer geliefert (`deep_sleep.png`, Projekt-Root) und gepackt — dabei einen echten Fehler im neuen `pack_image_asset.py` selbst gefunden, bevor er auslieferte: das Skript zielte zuerst auf die 800×480-Querformat-Zielgröße, aber jeder andere Bildschirm dieses Projekts wird im 480×800-Hochformat entworfen und erst ganz am Ende gedreht (siehe `blank()`/`startup()` in `generate_h3_assets.py`); das Nutzerbild (971×1619, Seitenverhältnis 0,600) passt nahezu exakt auf 480×800 (0,600) und wäre auf der falschen Zielgröße zu einem schmalen Streifen zwischen breiten weißen Rändern geschrumpft. Auf das Hochformat-Canvas mit anschließender Drehung umgestellt (identisch zum bestehenden Muster), danach volle, saubere Bildfläche ohne Zuschnitt. `generate_h3_assets.py` erzeugt „sleep“ nicht mehr mit; sonst hätte ein künftiger Sammel-Regenerierungslauf das handgestaltete Bild stillschweigend wieder durch den Platzhaltertext ersetzt. Zweiter Build/Flash mit dem finalen Sleep-Bild vom Nutzer als „perfekt“ bestätigt. — Erledigt 2026-09-11. Ein realer Niedrigakku-Durchlauf (5-%-Auto-Sleep) lässt sich nicht gezielt herbeiführen und bleibt offen, bis der Akku dort im normalen Betrieb ankommt; kein Abnahmekriterium für diesen Task.
- [x] A11-Recovery, Teilausschnitt `client_sessions.attention_required` (auf Nutzerwunsch vor dem allgemeinen A11-Vollumfang priorisiert): Anders als `processing_jobs.parked` (bereits per `jobs.py::queue_parked_jobs_for_night_repair` einmalig nachts wiederholt) hatte `client_sessions.state='attention_required'` aus `capture_intent_failed`/`knowledge_finalization_failed`/`clarification_answer_failed` überhaupt keinen Wiederholungsmechanismus — real beobachtet an Session 1119, die seit dem behobenen Whitespace-Bug unangetastet hängen blieb, weil nichts `finalize_client_session_with_knowledge` je erneut aufrief. Migration `0050_client_session_night_repair` ergänzt `client_sessions.night_repair_attempts` (0..1, wie beim bestehenden Pendant); `client_sessions.py::claim_attention_required_client_sessions_for_night_repair` beansprucht bis zu N Sessions einmalig, `retry_attention_required_client_sessions_for_night_repair` ruft für jede die bestehende `settle_client_session_for_ingestion` synchron auf (kein eigener Worker-Pool nötig, die Funktion besitzt bereits eigene Transaktionen und Fehlerbehandlung); in `run_daily_maintenance` eingehängt. Umfang bewusst auf `client_sessions.attention_required` begrenzt; Chat-/Promotion-Retries bleiben separate spätere Arbeit. Neuer Test `m8_client_session_night_repair_test.py` (registriert im M8-Gate, jetzt 28 Prüfungen) läuft gegen einen echten Capture→Segmentierung→Artefakt-Durchlauf: erzwingt `attention_required` künstlich, prüft Beanspruchung, Einmaligkeit, erfolgreiche Wiederherstellung auf `completed` und dauerhafte Eskalation nach einem bereits verbrauchten Versuch. Dabei eine echte Racebedingung gefunden und robust gemacht: Dieses Testverzeichnis läuft ohne eigene Test-Datenbank gegen dieselbe DB wie ein echter, parallel laufender `worker.py all`-Prozess — ein Job kann bereits von diesem übernommen sein, bevor der Test ihn selbst beansprucht (beobachtet: Job binnen Millisekunden `running` unter fremder Worker-ID). Der Test verlässt sich daher auf Polling des tatsächlichen Sessionzustands statt auf den Rückgabewert des eigenen Verarbeitungsversuchs. Vollständiges M8-Gate grün. **Live-Beobachtung, nicht behoben:** Session 1119 selbst ist inzwischen nicht mehr `attention_required`, sondern hängt jetzt bei `draining` — der ESP hat laut Audit-Trail `finish` wiederholt erneut gesendet (`attention_required`→`draining` um 20:09 UTC, ein weiteres `draining`→`draining` um 20:59 UTC), aber `reconciliation()` scheint seitdem kein `upload_complete` mehr zu melden. Das ist ein anderer, noch nicht verstandener Zustand als der hier behobene und kein A11-Abnahmekriterium; Session 1119 bleibt als Testbeleg unangetastet, die Ursache ist für eine spätere Aufgabe vorgemerkt.
- [x] Ursache für den bei A11 offen gebliebenen `draining`-Befund von Session 1119 gefunden und behoben: `finish_client_session()` regressierte bei einem erneut vom ESP gesendeten `finish` unbedingt zurück auf `draining` — der bestehende Schutz galt bisher nur für `old=="processing"`, nicht für `old=="attention_required"`. Session 1119 war zum Zeitpunkt des Retries (20:09 UTC) `attention_required` (capture_intent_failed, alter Whitespace-Bug); die Regression zu `draining` griff, `reconciliation()` meldete `upload_complete=True` (der einzelne Chunk war vollständig da), also rief der Code unbedingt `finish_ingestion_session_record()` auf — die aber wirft `ValueError`, sobald die zugrunde liegende `ingestion_sessions`-Zeile nicht mehr `open` ist. Genau das war hier der Fall: Die Ingestion-Pipeline hatte die Session unabhängig vom hängenden `client_sessions`-Zustand bereits bis `completed` durchlaufen. Der `ValueError` wurde nirgends gefangen (der Router fängt nur `ClientSessionConflict`), die Anfrage endete mit HTTP 500, und `client_sessions.state='draining'` blieb dauerhaft stehen, weil dieser Wert schon in einer eigenen, bereits committeten Transaktion vor dem Absturz geschrieben worden war — jeder weitere ESP-Retry (20:59 UTC) traf denselben Absturz erneut. Live in der Datenbank nachvollzogen (`client_session_audit` von Session 1119: `finish_requested attention_required→draining`, dann `draining→draining`, keine weitere Transition danach). Behoben mit zwei kleinen, lokalen Änderungen in `finish_client_session()`: (1) der bestehende Anti-Regressions-Schutz für `old=="processing"` gilt jetzt auch für `old=="attention_required"` — ein Retry auf einer bereits hängenden Session ist ein No-op statt einer Regression; (2) der Aufruf von `finish_ingestion_session_record()` erfolgt nur noch, wenn die Ingestion-Session tatsächlich noch `open` ist — andernfalls heilt der Code selbst in `processing` statt abzustürzen. Neuer Test `m8_client_session_finish_regression_test.py` (im M8-Gate registriert) fährt eine echte Capture bis `completed`, erzwingt danach exakt die beobachtete Session-1119-Konstellation sowie den allgemeineren Fall (bereits fortgeschrittene Ingestion-Session, `client_sessions` noch in `draining`) und beweist beide Regressionsfälle zuerst gegen den unreparierten Code (reproduziert exakt denselben `ValueError`), dann gegen den reparierten Code (grün). Vollständiges M8-Gate mit 29 Prüfungen einschließlich logischem Vier-Stunden-Soak grün. Session 1119 selbst blieb als Testbeleg unangetastet (nicht Teil dieser Änderung, ihr DB-Zustand demonstriert weiterhin den historischen Fehler). — Erledigt 2026-09-12.
- [x] A11/W06 Vollumfang geschlossen (Chat- und Promotionsretries, die der `attention_required`-Teilausschnitt bewusst offen gelassen hatte): Zwei weitere Fälle hatten keinen nächtlichen Wiederholungsmechanismus. Erstens `client_conversation_turns.status='failed'` — erholte sich bisher nur, wenn der Client selbst den bestehenden `retry_turn()`-Endpunkt aufrief. Zweitens `promote_session_artifacts()` (`services/promotion.py`) fängt eine Ausnahme je Artefakt intern ab und schreibt sie nach `session_artifacts.promotion_error`, wirft sie aber nie weiter — die zugehörige `client_session` erreicht trotzdem `completed`, und dieses eine liegengebliebene Artefakt ist für die bestehende `client_sessions`-Nachtreparatur unsichtbar, weil die nur den Sessionzustand prüft. Migration `0051_chat_and_promotion_night_repair` ergänzt `night_repair_attempts` (0..1) auf beiden Tabellen, exakt wie beim bestehenden `client_sessions`-Pendant. `client_chat.py::queue_failed_chat_turns_for_night_repair` requeued fehlgeschlagene Turns genau einmal (Zustand zurück auf `queued`, Konversation zurück auf `processing`) — die eigentliche Wiederholung übernimmt danach unverändert `worker.py`s bestehender `chat`-Worker. `promotion.py::claim_promotion_error_artifacts_for_night_repair`/`retry_promotion_errors_for_night_repair` beanspruchen promotionsfehlerhafte, noch nicht promovierte, bestätigte Artefakte einmalig und rufen `promote_session_artifacts()` gruppiert je Session mit genau diesen Artefakt-IDs erneut auf. Alle drei Mechanismen (Client-Sessions, Chat-Turns, Promotion) sind jetzt in `run_daily_maintenance` eingehängt. Neuer Test `m8_chat_promotion_night_repair_test.py` (im M8-Gate registriert, jetzt 30 Prüfungen) prüft je Fall Beanspruchung, Einmaligkeit, erfolgreiche Wiederherstellung und dauerhafte Eskalation nach bereits verbrauchtem Versuch; gegen den unreparierten Code reproduziert er einen `ImportError` (Beweis, dass der Test die neuen Funktionen tatsächlich prüft), gegen den reparierten Code ist er grün. Für den Promotionsfall wird die betroffene `session_artifacts`-Zeile bewusst direkt aufgebaut statt über die volle Memo-Capture-Pipeline, da reiner Fließtext im deterministischen Testmodus nicht zuverlässig in einen Segmenttyp klassifiziert; für den Chat-Fall ergab sich dieselbe Racebedingung mit einem parallel laufenden echten `worker.py all`-Prozess wie bei der vorherigen `client_sessions`-Nachtreparatur, gelöst mit demselben Polling-statt-Rückgabewert-Muster. Vollständiges M8-Gate grün. `BACKEND_LOGIK.md` A10/A11/W06 auf vollständig geschlossen aktualisiert. — Erledigt 2026-09-12.

- [x] Zwei reale, vom Nutzer nach einem Worker-Neustart und frischem ESP-Flash aufgenommene Testfälle blieben im Verlauf dauerhaft bei „in Bearbeitung" hängen, obwohl das LLM idle war (Sessions 1388 „Fotos sortieren"/Liste „Ausgaben" anlegen und 1390 „Lösche die Liste Fotos sortieren"). Zwei unabhängige, echte Backendfehler gefunden und behoben. **Erstens**, Session 1388: `stabilize_transcripts()` (`services/audio.py`) wird nur aus dem Abschlusshook des jeweils letzten `audio_transcription`-Jobs heraus aufgerufen und bekommt dort `force=session_status=='finished' and later_pending is None` übergeben. Bei einer kurzen Aufnahme mit genau einem STT-Fenster kann die Transkription abschließen, *bevor* `finish_ingestion_session_record()` den Sessionstatus auf `finished` setzt — live an Session 1388 nachvollzogen (Job fertig 09:35:25.367, Session erst 09:35:26.412 `finished`). Mit `force=False` bleibt bei genau einem Fenster nichts übrig, das bestätigt wird (`cutoff=contiguous-1=0`); das Transkript blieb für immer `provisional`, nie zu `ingestion_chunks` materialisiert, nie zu `text_processing`/`session_artifacts` weitergereicht. `finish_client_session()`s eigener `schedule_final_stt_windows()`-Aufruf danach ist für ein bereits abgeschlossenes Fenster nur ein wirkungsloser Replay derselben Idempotenzschlüssel. Behoben: `finish_client_session()` ruft direkt danach, sofern kein `audio_transcription`-Job für diese Session mehr `queued`/`running` ist, zusätzlich `stabilize_transcripts(ingestion_id,force=True)` auf — derselbe Schutz vor verfrühtem Erzwingen (keine noch laufende Transkription, deren Overlap-Revision das sonst einfrieren würde) wie im ursprünglichen Aufruf, nur nachgeholt für genau die Session, die die Bestätigung sonst nie bekommen hätte. Neuer Test `m8_stt_finish_race_test.py` fährt real drei Audiochunks, verarbeitet den STT-Job bewusst vor `finish` (reproduziert die Race, Segment bleibt nachweislich `provisional`, `ingestion_chunks` bleibt leer), ruft dann `finish_client_session()` auf und beweist, dass Segment und Chunk jetzt entstehen und die Session automatisch bis `completed` durchläuft; gegen den unreparierten Code schlägt genau diese letzte Prüfung fehl (Segment bleibt `provisional`), gegen den reparierten Code ist er grün. **Zweitens**, Session 1390: Der Job-Graph (`audio_transcription`→`text_processing`→`session_artifacts`) lief vollständig bis `done` durch, `client_sessions.state` blieb aber bei `processing` mit `last_error=NULL` stehen. Ursache: `settle_client_session_for_ingestion()` schluckt `ClientSessionConflict` aus `finalize_client_session_with_knowledge()` laut eigenem Docstring bewusst still, in der Annahme, „etwas anderes" würde die Session später erneut anstoßen — das tut aber nichts, denn ein wiederholtes `finish` vom ESP ist bei bereits `processing`/`attention_required` explizit ein No-op, und die bestehende A11-Nachtreparatur beansprucht ausschließlich `state='attention_required'`. Manuelles erneutes Aufrufen von `finalize_client_session_with_knowledge` auf der echten Session schloss sie sofort fehlerfrei ab (archivierte dabei tatsächlich die Liste „Fotos sortieren", exakt die vom Nutzer beabsichtigte Aktion) — die ursprüngliche Blockade war transient, aber nichts hätte je einen zweiten Versuch unternommen. Behoben mit einem vierten Fall in derselben A11-Familie: `client_sessions.py::claim_stalled_processing_client_sessions_for_night_repair`/`retry_stalled_processing_client_sessions_for_night_repair` holen `processing`-Sessions mit vollständig `done`-gelaufenem Job-Graph (kein einziger `processing_jobs`-Datensatz mehr ungleich `done`) und mindestens zehn Minuten unverändertem `updated_at` genau einmal nach; bleibt die Session danach bei `processing`, eskaliert sie auf `attention_required` mit auf `0` zurückgesetztem `night_repair_attempts` — ein echter Übergang an die bestehende A11-Reparatur für einen zweiten, unabhängigen Versuch statt einer zweiten stillen Sackgasse. In `run_daily_maintenance` eingehängt. Neuer Test `m8_stalled_processing_night_repair_test.py` prüft Beanspruchung, Einmaligkeit, den Zehn-Minuten-Mindestabstand, erfolgreiche Wiederherstellung über den kombinierten Claim-plus-Retry-Einstiegspunkt sowie die Eskalation für einen künstlich dauerhaft blockierten Fall (erzwungen über ein zu hoch gesetztes `expected_final_sequence`, damit `upload_complete` nie wahr wird). Beide Tests laufen gegen die reale Datenbank; dabei eine bereits bestehende, unabhängige Randbedingung erneut bestätigt: `CLIENT_DEVICE_AUTH_REQUIRED=true` steht aktuell in `.env` (vermutlich vom Nutzer für die reale Geräteabnahme gesetzt) und lässt mehrere ältere, nicht mit dieser Aufgabe zusammenhängende M8-Tests (u. a. `m8_client_session_night_repair_test.py`, `m8_client_session_finish_regression_test.py`) mit `401 DEVICE_CREDENTIAL_INVALID` fehlschlagen, weil sie keinen Operator-/Geräte-Header senden; die beiden neuen Tests hier setzen den Operator-Bearer-Header bewusst, die bestehenden nicht — nicht in dieser Aufgabe behoben, da außerhalb ihres Zuschnitts und potenziell viele Dateien betreffend. `BACKEND_LOGIK.md` A11/W06 aktualisiert. — Erledigt 2026-09-12.

- [x] Nach obigem Fix zeigte sich: Session 1388 stand zwar auf `completed`, aber weder im Dashboard noch unter „Listen“ erschien die diktierte Liste „Ausgaben“ mit ihren drei Einträgen — stattdessen eine einzelne, sichtbar abgeschnittene Aufgabe „… soll eingetragen werden“. Drei getrennte, echte Klassifikationsfehler in der Kette Segmentierung→Regelrouter→LLM-Vorschlag→Promotion gefunden und behoben, alle an derselben realen Session nachvollzogen. **Erstens**, `services/artifacts.py::_router_overrides()`: Die Segmentierung hatte den Satz bereits korrekt in ein `list_candidate`- plus drei `list_item_candidate`-Segmente zerlegt, aber der Regelrouter (`semantic_router.py::route_artifact()`) erkennt nur zwei feste Listen-Erstellungsformulierungen und übersah „nenne die Liste Ausgaben“ komplett; da der Satz zusätzlich „soll“ enthält, wurde er mit 0,86 Konfidenz als Aufgabe angelegt — noch bevor das LLM, das die Formulierung verstanden hätte, überhaupt zum Zug kam. Die drei isolierten Item-Segmente ohne Listenkontext verwarf das LLM daraufhin stillschweigend. Behoben: `_router_overrides()` überspringt die Regel-Klassifikation jetzt, wenn ihr Ergebnis der von der Segmentierung bereits vergebenen `list_candidate`/`list_item_candidate`-Einordnung widerspricht, und überlässt diesen Fall stattdessen dem kontextbewussten LLM-Schritt. **Zweitens**, `services/content_types.py::validate_classification()`: Selbst mit korrekter Listenklassifikation verlangte die Validierung für jeden LLM-vorgeschlagenen Listeneintrag zwingend `normalized_data.target_list`/`items` — eine reine Stapelform, die nur der Regelrouter befüllt (alle Items eines Satzes in einem Aufruf) und die `promote_session_artifacts()` für den LLM-Pfad ohnehin nie liest (der nutzt das verknüpfte Thema, nicht `normalized_data`). Das reale LLM lieferte dieses Feld zudem uneinheitlich (`parent_list`, dann gar nichts) — die Items wurden dadurch grundsätzlich verworfen. Gelockert: ein LLM-Einzeleintrag ohne `items`-Array braucht nur noch ein nicht-leeres `content`. **Drittens**, `services/promotion.py::promote_session_artifacts()`: Selbst mit angelegtem Item wurde „Kette für nach dem M2“ der falschen, thematisch ähnlich klingenden Bestandsliste „Nach dem M2“ zugeordnet statt „Ausgaben“ — weil `explicit_target` nur bei den beiden regelspezifischen `reason_codes` `explicit_list_target`/`implicit_list_context` gesetzt wird, nie bei `list_item_candidate` (dem LLM-eigenen Code), wodurch `process_list_item_candidate()` in die inhaltsbasierte Ähnlichkeitssuche über alle Listen fiel statt dem bereits verknüpften Thema zu vertrauen. `list_item_candidate` ergänzt. Reale Session 1388 direkt nachbearbeitet: die kaputte Aufgabe archiviert, die Liste „Ausgaben“ mit allen drei korrekten Einträgen angelegt, der fehlgeleitete Eintrag aus „Nach dem M2“ entfernt. Neue Regression in `semantic_router_test.py` (Teil des B6-Gold-Tests) reproduziert alle vier Originalsegmente wortgleich und beweist sowohl die Router- als auch die Validierungskorrektur direkt gegen `_router_overrides()`/`validate_classification()`, ohne LLM-Abhängigkeit; gegen den unreparierten Code schlägt die Router-Prüfung mit exakt dem beobachteten Aufgaben-Fehlgriff fehl. Die Promotion-Korrektur (dritter Fehler) hängt an echter, nicht-deterministischer LLM-Ähnlichkeitssuche und ist nur an der realen Session verifiziert, nicht separat automatisiert getestet. `semantic_router_test.py`, `m8_content_type_pipeline_test.py` und die angrenzenden Mutation-/Capture-Tests bleiben grün. — Erledigt 2026-09-12.

- [x] ESP32-Uploadqueue: echtes, persistiertes Pro-Segment-Backoff für `retry_class=backoff` ergänzt — auf Nutzerwunsch erster von vier priorisierten Teilpunkten des offenen H2-Uploadqueue-Eintrags (Reihenfolge: Retryklassen → Stromausfalltests → Credentialrotation → Verschlüsselung zuletzt). Vorher teilten sich alle `ready`-Segmente unabhängig von `retry_class` denselben festen ~5-Sekunden-Takt; `docs/CLIENT_SERVER_STATE.md` Punkt 3 dokumentierte das ausdrücklich als bewusst zurückgestellt, weil es einen weiteren persistierten Pro-Chunk-Zustand gebraucht hätte. Genau den gibt es jetzt: neuer Journal-Recordtyp `chunk_backoff` (`journal.h`/`journal.c`, Felder `ba`=Versuchszähler, `bu`=Unix-Sekunden-Deadline) neben dem bestehenden `chunk_state`. Jede gewöhnliche `chunk_state`-Transition setzt beide Felder zurück, live wie beim Replay, damit ein später unabhängig zurückgesetztes Segment (z. B. `resync_missing()`s `server_missing`) keine veraltete Backoff-Deadline mitschleppt. `api_client.c::upload_chunk()` behandelt `backoff` jetzt als eigenen Zweig (`mark_chunk_backoff()`): exponentiell wachsend (30 s Boden, 30 min Deckel, ±20 % Jitter, sechs Versuche bis zum Deckel erreicht ist), `transfer_session()` überspringt ein Segment, solange `now() < backoff_until`, ohne die übrigen Segmente derselben Session zu blockieren. Wall-Clock (`time(NULL)`), nicht die monotone Bootuhr: eine monotone Deadline würde einen Reboot nicht überleben, weil `esp_timer` dabei nahe null neu startet und eine alte, größere gespeicherte Deadline nie wieder einholen könnte — das Segment bliebe scheinbar für immer zurückgestellt. Die Wartezeit wird nur durchgesetzt, wenn `clock_ready()` wahr ist; ohne synchronisierte Uhr bleibt das alte Verhalten (jeden Takt versuchen) erhalten, statt eine nicht berechenbare Wartezeit zu erzwingen. `network`/unklassifiziert/unlesbar bleiben bewusst unverändert auf dem gemeinsamen Takt, wie in `API_INTERACTION.md`s Segmentupload-Tabelle spezifiziert.

  Verifiziert über eine eigene, nicht eingecheckte Hostprüfung (Rundtrip von `journal_build_chunk_backoff` durch `journal_apply_buffer`, Reset bei gewöhnlicher Transition, Überleben eines simulierten Reboots via frischem `journal_replay()` auf ein neues Struct — 23 Prüfungen grün), da der bestehende `tools/journal_test.c`/`run_journal_test.sh` auf dieser nativen Windows-Toolchain nicht durchläuft. Nicht durch diese Änderung verursacht: `journal_test.c`s `main()` verlässt sich auf `system("rm -rf ... && mkdir -p ...")` und später `cp ... .bak`, und die native mingw-EXE, `cmd.exe`/Git-`cp.exe` und die aufrufende Git-Bash lösen einen bloßen `/tmp/...`-Pfad jeweils unterschiedlich auf (drei verschiedene Zielverzeichnisse für denselben String) — dieselbe Klasse von nur-unter-Linux/WSL/macOS-lauffähiger Testlücke, die für `tools/ui_test.c`s `setenv()` bereits in diesem Dokument steht. Vollständiger `idf.py build` (ESP-IDF 5.5.2, esp32s3) kompiliert `journal.c` und `api_client.c` sauber durch. Noch nicht geflasht/live geprüft — ein echtes `retry_class=backoff` müsste der Server absichtlich liefern, was ohne Backend-Mitwirkung nicht erzwingbar ist, dieselbe Einschränkung wie bei den bereits dokumentierten `immediate`/`never`/`user_action`-Pfaden. `docs/CLIENT_SERVER_STATE.md` Punkt 3 aktualisiert. — Erledigt 2026-09-12.

- [x] ESP32-Uploadqueue: ACK-/Schreibgrenzen-Stromausfalltests real zum Laufen gebracht — zweiter der vier priorisierten H2-Teilpunkte. `tools/journal_test.c` deckte Recordframing, jede Recovery-Klassifizierung und Stromausfall an jeder Byteposition eines echten Journals bereits ab, lief auf dieser nativen Windows-Toolchain aber noch nie durch — genau die Testlücke, die der Backoff-Eintrag oben als vorbestehend und nicht behoben dokumentiert hatte. Jetzt geschlossen statt umgangen: `tools/run_journal_test.sh`/`journal_test.c` shellten für Setup und die Byteschnitt-Sweeps über `system("rm -rf ...", "cp ...")`, was mingw-EXE, `cmd.exe` und die aufrufende Git-Bash je unterschiedlich auflösten; ersetzt durch reines C (rekursives Löschen/Anlegen über `opendir`/`unlink`/`rmdir`/`mkdir`, Byteschnitte über einen einmal eingelesenen Speicherpuffer statt `cp`-Backup), `mkdir` mingw-kompatibel gemacht (kein Mode-Argument dort) und ein neuer Host-only-Shim `tools/host_compat.h` (per `-include` erzwungen) ergänzt `fsync` als `_commit`, weil ESP-IDFs newlib das POSIX `fsync` bereitstellt und mingw nicht. Dabei einen echten, vorher unentdeckten Fehler in `main/journal.c` selbst gefunden: keiner der fünf rohen `open()`-Aufrufe des Journals setzte `O_BINARY`. Auf dem Gerät folgenlos (ESP-IDFs VFS kennt keinen Text-/Binärmodus-Unterschied), aber ein natives Windows-`open()` ohne dieses Flag läuft im CRT-Textmodus und übersetzt rohe `0x0A`-Bytes still beim Schreiben/Lesen — die in Längen- und CRC-Feldern unvermeidlich vorkommen. Fiel erst beim bestehenden 32-Segment-Testfall auf, sobald das Journal über ein einzelnes 4096-Byte-Lesefenster hinauswuchs. Mit `O_BINARY` (als `0` definiert, also No-op außerhalb von Windows) an allen fünf Stellen behoben; `idf.py build` bleibt unverändert grün. Neuer, gezielter Testfall `test_ack_write_boundary()` deckt zusätzlich genau die im Aufgabentext benannte ACK-Grenze ab: baut die reale `api_client.c::upload_chunk()`-Sequenz nach (`CHUNK_UPLOADING` vor dem Request, `CHUNK_ACKED` nach passendem `durable_ack`) und schneidet nur diesen einen Übergangsrecord an jeder Byteposition — ein zerrissener ACK-Record wird nie als „acked“ geglaubt, fällt auf `CHUNK_UPLOADING` zurück und landet über `journal_recover()` in einem reinen Retry (`CHUNK_READY`), nie in `attention`; das unberührte Nachbarsegment bleibt bei jedem Schnitt exakt unverändert. `sh tools/run_journal_test.sh` läuft jetzt lokal grün (9576 Prüfungen; ohne ASan/UBSan, da dieser mingw-Toolchain die Sanitizer-Laufzeitbibliotheken fehlen — das Skript erkennt das automatisch und baut ohne sie weiter). `esp32-client/docs/CLIENT_SERVER_STATE.md` Punkt 3 und `CHANGELOG.md` aktualisiert. — Erledigt 2026-09-12.

- [x] ESP32-Uploadqueue: automatische Credentialrotation ergänzt — dritter der vier priorisierten H2-Teilpunkte. Serverseitig war die idempotente Zwei-Phasen-Rotation (`services/device_auth.py::rotate()`) bereits vollständig, siehe `BACKEND_REQUIREMENTS.md` §9; der ESP stieß sie bisher nie von sich aus an. `main/api_client.c::enroll_if_needed()` liest jetzt zusätzlich das serverseitige `rotate_after` aus der Enrollment-Antwort und merkt es in NVS; die neue `rotate_if_due()` wird in jedem `synchronize()`-Durchlauf direkt nach `enroll_if_needed()` und vor den beiden Gate-Aufrufen geprüft und ruft bei Fälligkeit `POST /installations/{id}/credentials/rotate` auf. Dieselbe Durable-vor-Senden-Disziplin wie beim Chunk-ACK in `journal.c`: die `request_id` wird vor dem Netzwerkaufruf in NVS committed, sodass ein Stromausfall zwischen Versand und persistierter Antwort beim nächsten Zyklus mit derselben `request_id` erneut versucht wird und über die serverseitige Idempotenz dieselbe neue Credential zurückerhält, statt eine überzählige zu erzeugen — unabhängig davon, ob die alte oder schon die neue Credential zum Zeitpunkt des Absturzes in NVS stand, authentisiert sich beide noch, weil `rotate()` die alte Credential erst revoziert, sobald die neue tatsächlich benutzt wurde; das übernehmen die beiden Gate-Aufrufe im selben Durchlauf. `rotate_after`/`expires_at` kommen als `Europe/Berlin`-Zeitstempel mit Offset, nicht als UTC; ein neuer lokaler `approx_unix_from_iso()` parst bewusst nur auf Minutengenauigkeit (Sekunden und Offset ignoriert, dieselbe Toleranz wie `history.c`s Anzeige-Parser) — bei einem 60-Tage-Rotationsfenster unerheblich. Eigenständig, nicht eingecheckt gegen acht Fälle geprüft (Epoche, Rundung, 32-Bit-Grenze, leere/kaputte/fehlende Eingabe, fehlendes `T`), 8/8 grün. Zwei neue `diagnostic_log`-Ereignisse ergänzt. `idf.py build` grün. Eine reale Rotation am Gerät ist nicht praktikabel erzwingbar (60 Tage Wartezeit oder eine künstlich vorgezogene DB-Deadline), dieselbe Einschränkung wie bei den bereits dokumentierten übrigen Retry-Pfaden. Ein vorbestehender, unabhängiger Zustand bleibt bewusst unangetastet: eine bereits vollständig widerrufene Credential hat weiterhin keinen automatischen geräteweiten Wiederherstellungspfad, da ein erneutes Enrollment einen neuen Einmalcode bräuchte, den das Gerät nicht mehr besitzt — das ist derselbe Fall, den der spätere Systemaudit-Punkt in dieser Datei ohnehin abdeckt. `esp32-client/docs/BACKEND_REQUIREMENTS.md` §9 und `docs/CLIENT_SERVER_STATE.md` Punkt 3 sowie `CHANGELOG.md` aktualisiert. — Erledigt 2026-09-12.

- [x] ESP32-Uploadqueue: Verschlüsselung ergänzt — vierter und letzter der vier priorisierten H2-Teilpunkte. `journal.h`/`journal.c` trugen die dafür vorgesehenen Felder (`plain_`/`stored_length`/`_sha256`, `enc`) schon lange als Platzhalter; `journal_chunk` bekommt jetzt zusätzlich `iv`/`tag` (Hex-codiert), und ein neuer Callback-Typ `journal_encrypt_fn` — injiziert exakt wie das bestehende `journal_hash_fn` — hält `journal.c` weiterhin frei von Plattform-Crypto. Neues Modul `main/crypto.c`/`.h`: pro Installation ein zufälliger 256-Bit-Schlüssel in NVS (Namespace `notebook`, wie beim Geräte-Credential; Flash Encryption schützt das erst bei der Produktionsfreigabe, separat priorisiert). `audio_crypto_encrypt_file()` verschlüsselt ein fertiges Segment mit AES-256-GCM (Zufalls-IV, `session_id:sequence` als Associated Data gegen Segment-Vertauschen) in eine Sibling-Datei, fsynct, ersetzt dann das Klartext-`.M4A`. Alle drei Stellen, die bisher aus einem fertigen Klartextsegment einen `chunk_ready`-Record mit `enc="none"` bauten (`recorder.c::record_memo()`, `journal_recover()`s `CHUNK_WRITING`-Ast, `journal_adopt()`), rufen jetzt zusätzlich diesen Callback auf. Der Uploadpfad verifiziert den GCM-Tag über die komplette Chiffratdatei, bevor überhaupt eine Verbindung geöffnet wird — ein beschädigtes At-rest-Chiffrat landet sofort lokal auf `attention`/`hash`, statt einen Uploadversuch zu verschwenden — und entschlüsselt danach blockweise direkt in den Sendepuffer (kein Klartext auf SD, keine volle RAM-Pufferung). Bereits vorhandene `enc="none"`-Segmente laufen unverändert über den alten Klartextzweig, kein harter Cutover. Dabei einen echten, sonst stillen Regressionsfall gefunden und im selben Zug behoben: der USB-Diagnoseexport (`recorder.c::export_memo()`, `memo-files`) hätte ab jetzt unbemerkt Chiffrat statt Klartext über die serielle Schnittstelle geschickt; liest jetzt den Journal-Eintrag jedes Segments und entschlüsselt wie der Uploadpfad. `journal_build_chunk_ready()`/`apply_payload()` schreiben/lesen `iv`/`tg` nur, wenn gesetzt — ein `enc="none"`-Record bleibt byteidentisch, keine Journalmigration. `tools/journal_test.c` bekam einen no-op-`fake_encrypt()` für alle 13 bestehenden `journal_recover()`/`journal_adopt()`-Aufrufe plus neue Prüfungen für `enc`/`iv`/`tag` nach echtem Disk-Replay; `sh tools/run_journal_test.sh` lokal grün (9580 Prüfungen, vier neu). Vollständiger `idf.py build` (ESP-IDF 5.5.2, esp32s3) kompiliert sauber durch, inklusive `crypto.c` als neue Komponente.

**Erster echter Gerätetest schlug fehl, echter Fehler gefunden und behoben:** jede Aufnahme brach sofort mit `segment encryption failed` ab (sichtbar am Gerät als „Achtung — lokale Diagnose über USB öffnen"). Ursache: `mbedtls_gcm_update()` garantiert auf diesem Ziel nicht, dass die Ausgabelänge pro Aufruf der Eingabelänge entspricht — laut dem vendorierten `components/mbedtls/mbedtls/include/mbedtls/gcm.h` puffert sie intern blockweise (das `acceleration`-Feld im Kontext deutet auf die S3-Hardwarebeschleunigung als Ursache hin), und `mbedtls_gcm_finish()` kann am Ende zusätzlich bis zu 15 Byte Restdaten liefern. Diese beiden Eigenschaften lassen sich nicht gegen den Compiler prüfen, nur am realen Gerät. Der ursprüngliche Code prüfte `produced != got` als Fehlerbedingung (falsch) und verwarf `mbedtls_gcm_finish()`s Ausgabeparameter mit `NULL, 0` (hätte am Dateiende Bytes verloren). Behoben in `audio_crypto_encrypt_file()`, `audio_crypto_verify_file()` und `audio_crypto_reader_read()`: Ausgabepuffer mit `Eingabelänge + 15` Headroom (mbedtls-Untergrenze), `produced`/`tail_len` als tatsächliche Längen übernommen statt geprüft, echter Ausgabepuffer für `mbedtls_gcm_finish()`. `audio_crypto_reader` bekam dafür einen internen Pending-Puffer, da ein Aufrufer pro `read()` eine feste, selbst gewählte Byteanzahl erwartet, GCM aber pro internem Schritt eine davon unabhängige Menge liefert. `idf.py build` bleibt grün, `sh tools/run_journal_test.sh` unverändert grün (reines Host-C ohne mbedtls-Abhängigkeit, von diesem Fehler nicht betroffen). **Zweiter echter Gerätetest, gleiches äußeres Bild, anderer echter Fehler:** nach obigem Fix schlug die Aufnahme mit identischem Fehlerbild erneut fehl. Gezieltes Stufen-Logging (jede mbedtls-/Datei-Operation in `audio_crypto_encrypt_file()` loggt jetzt einzeln mit Fehlercode/`errno`) fand ihn sofort: `cannot open ciphertext temp file, errno=22` (`EINVAL`). Ursache: jeder von dieser Firmware geschriebene Dateiname ist ein strikter FAT-8.3-Kurzname (`00000000.M4A`, `JOURNAL.LOG`, …); `path + ".ENC"` erzeugte `00000000.M4A.ENC` — zwei Punkte, zu lang, von FATFS mit `EINVAL` abgelehnt, nur am echten Gerät sichtbar. Behoben: Endung wird per `strrchr(path,'.')` ersetzt statt angehängt, Ergebnis `00000000.ENC`. `idf.py build` grün, `sh tools/run_journal_test.sh` unverändert grün (crypto.c ist nicht Teil des Hosttests). **Dritter Gerätetest nach beiden Fixes: erfolgreich.** Reale Aufnahme lief durch (`capture SAVED; segments=1`), wurde hochgeladen und vom Server bestätigt (`acked=1 ... finish=1` im Sync-Log). Damit ist H2 vollständig — alle vier priorisierten Teilpunkte real am Gerät bestätigt. `esp32-client/docs/CLIENT_SERVER_STATE.md` Punkt 3 aktualisiert. — Erledigt 2026-09-12.


- [x] Backend-Auto-Modus A04: Ein gemeinsamer Typvertrag unterscheidet materialisierbare Session-Artefakte, separat gespeicherte Questions und Fact-/Decision-Kandidaten für Claims; Segmentierung, Router, Shadow, API-Schemas, direkter Capture, Tageskonsolidierung und Artefaktvalidierung verwenden dieselben Definitionen und lokalen Guards. `list_candidate` ist schema- und migrationsfest ergänzt. Tasktitel werden kurz und ohne relative Zeitangaben erzeugt; „heute Nachmittag“ wird relativ zum Aufnahmezeitpunkt als 12:00–18:00 normalisiert. Die nach der ersten Audioabnahme nötige Listenstabilisierung ist automatisiert und mit vier echten Audio→DB→ESP-Proben bestätigt. — Erledigt 2026-09-10.

- [x] Backend-Auto-Modus A02: Nach dem gemeinsamen Text-/Audiopfad wird `auto` inhaltlich und strukturiert als `memo|query|change|complete|archive` entschieden und pro Session persistiert. Zieltyp/-text, Sicherheit, kontrollierte Gründe und Mehrfachkennzeichen bleiben erhalten. Änderungsabsichten werden bis A06/A07 nicht ausgeführt und nicht als neue Inhalte promotet; gemischte Eingaben warteten bis zum anschließenden A03-Schnitt mit `pending_split`. Deterministischer Vertragstest deckt alle Intentklassen und die Promotionssperre ab; echter LLM-Schemaaufruf und vollständiges M8-Gate sind grün. — Erledigt 2026-09-10.

- [x] Backend-Auto-Modus A03: Gemischte `auto`-Eingaben werden in höchstens zwölf geordnete, inhaltlich vollständige und semantisch segmentgebundene Quellspannen zerlegt; zwischen den exakten Spannen darf nur bedeutungsfreier Leerraum liegen. Nur ausschließlich memo-gestützte Artefakte gelangen zur Promotion; Frageanteile bilden in Quellreihenfolge den Query-Turn, Mutationen bleiben `pending_resolution`. Öffentliche Ergebnisse enthalten exakte Zeichenbereiche, aber keine internen Segment-IDs. Deterministischer Vertragstest, echter verschachtelter LLM-Schemaaufruf und vollständiges M8-Gate samt logischem Vier-Stunden-Soak sind grün. — Erledigt 2026-09-10.

- [x] ESP32-Akkuanzeige aus dem realen PMIC gespeist: Waveshare-Schaltplan und Boardbeispiel nennen AXP2101, aktuelle Produktprosa TG28; das reale Board beantwortet den dokumentierten AXP2101-Leseteilsatz an `0x34` kohärent. Nur Status, vorhandener Enable-Zustand, VBAT und E-Gauge werden gelesen, keine Register beschrieben. Reale USB-Probe: Akku vorhanden, Laden aktiv, Gauge aktiv, zunächst 99 % bei 4179–4180 mV und später 100 % bei 4193 mV; Akkusymbol und Prozentzahl sind real gut lesbar bestätigt. Unplausible/fehlende Werte bleiben sichtbar unbekannt. Build und Flash auf COM9 grün. Vollständige Zyklen bleiben spätere Genauigkeits-/Laufzeitkalibrierung. — Erledigt 2026-09-09.
- [x] ESP32-Ladeanzeige: PMIC-Ladestatus wird alle fünf Sekunden gelesen; bei aktivem Laden steht ein Blitz in der Batteriezelle, die Prozentzahl bleibt sichtbar. Status-Demo, ESP-IDF-Build, Flash auf COM9 und reale Sichtprobe grün. — Erledigt 2026-09-10.
- [x] ESP32-Aufnahme auf BOOT verlegt: GPIO0 startet beim ersten erkannten Druck ohne 500-ms-Haltephase und beendet beim Loslassen; die Mitteltaste ist nur noch Auswahl/Zurück. Mindestdauer 1,5 Sekunden, Journal, Upload und Fünf-Minuten-Grenze bleiben unverändert. ESP-IDF-Build und Flash auf COM9 grün; physische Sprachprobe nach dem Flash bleibt als letzte Sichtprüfung möglich. — Erledigt 2026-09-10.
- [x] ESP32-Statusleiste um das lokale Datum `D.M.YY` neben der Uhrzeit ergänzt; ohne führende Nullen für Tag/Monat und ohne geratenes Datum vor Zeitsynchronisation. ESP-IDF-Build, Flash auf COM9 und reale Sichtprobe nach Angleichung von Font und Grundlinie sind grün. — Erledigt 2026-09-09.
- [x] ESP32-Home-Dashboard fertiggestellt: Home-Auswahl (offene Rückfragen, Processing-Hinweise, höchstens drei serverseitig ausgewählte nächste Task-/Listenentitäten), echter nicht fokussierbarer `alert`-Renderer und Fokus-Erhalt über `component.id`/`entity_ref`; gezielter Backend-/Wire-Test, vollständiges M8-Gate, ESP-IDF-Build, Flash und reale Sicht-/Fokusprobe grün. Keine technischen Processingkarten oder lokalen Fachregeln. — Erledigt 2026-09-09.
- [x] ESP32-Verlauf zeigt den technischen Sessionstand direkt pro Zeile mit Zustandsicon, lokaler Zeit und deutschem Kurzstatus; Mitteldruck aktualisiert statt eine leere Session-Detailansicht zu öffnen. Falscher Listenmarker beim Wechsel in den Verlauf ist korrigiert. ESP-IDF-Build, Flash auf COM9 und reale Sicht-/Navigationsprobe sind grün. Ein manueller Abbruch bei Fehler/Aufmerksamkeit bleibt nur als spätere Option vorgemerkt. — Erledigt 2026-09-09.
- [x] Erste ESP32-Einstellungsstufe: lokale fünfte Settings-Shell mit Rückkehr zur vorherigen Ansicht, sichtbaren Zeilen für WLAN hinzufügen, Diagnose, SD-Logs und Zeitzone sowie inhaltsarmer Diagnose (Software, Contract/Gate, Netz, Queueklassen, Speicher) implementiert. Symbolatlas, ESP-IDF-Build, Flash auf COM9 und reale Navigation-/Lesbarkeitsprobe sind grün. — Erledigt 2026-09-09.
- [x] ESP32-Einstellungen: „WLAN hinzufügen“ besitzt einen dauerhaft geöffneten temporären Hotspot, WLAN-only-Portal, geräteseitigen/Browser-Abbruch ohne Konfigurationsänderung und Speicherung von bis zu fünf automatisch durchprobierten Profilen. Zwei reale Netze, wiederholte Umschaltung, Rückfall bei abgeschaltetem Handyhotspot und Live-Speichern ohne Verlust des vorhandenen Dashboards sind am Gerät bestätigt. — Erledigt 2026-09-09.
- [x] ESP32-Einstellungen: begrenzter, rotierter und technisch bereinigter SD-Diagnoselogsink sowie die Scrollmechanik des SD-Logviewers sind implementiert. Sink auf realer SD-Karte, Build/Flash auf COM9 sowie Sicht- und Rückkehrprobe sind bestätigt; eine spätere reale Nutzung meldete das Scrollen jedoch als wirkungslos und hat die UI-Abnahme im offenen Sichtkorrekturblock wieder geöffnet. `MEMOS/*/JOURNAL.LOG` bleibt Aufnahmezustandsjournal, kein Nutzerlog. — Technischer Unterbau erledigt 2026-09-09, UI-Abnahme wieder offen.

- [x] ESP32-Dashboard und Listenansicht verfeinern: nicht gerenderte Komponenten/leere Überschriften entfernt, technische Sessionkarten auf den Verlauf begrenzt, Listenanzahl verständlich dargestellt und Listendetails als scrollbare, einzeln anhakbare, ausfallsicher journalisierte Items umgesetzt. Reale Memo→Liste→ESP- sowie Toggle/Zurücktoggeln→Server→ESP-Probe bestanden; „Hafermilch“ verschwand nach bestätigtem Versand. Shared-DB-Testfixtures räumen auch bei Fehlschlag auf. — Erledigt 2026-09-08.

- [x] ESP32: dauerhafte Uploadqueue mit Recovery, Verschlüsselung und vertraglichem API-Client. H2-Entwicklungspfad gegen Mock und echtes FastAPI-Backend am Gerät bestanden: 0-basierte ESP-Wire-Sequenzen werden über das unveränderliche Top-Level-Feld `sequence_base` deklariert, Quick-Captures `memo|auto` laden ohne Meeting-`start`, durable ACK/Reconciliation/Finish sind nachgewiesen. Backendseitig sind Create-Identität, paginierte Sessionhistorie, stabile Fokus-IDs, SSE, Enrollment/Rotation und die monotone sessionsweite Audiofreigabe abgeschlossen und im M8-Gate. Firmwareseitig sind HTTPS-Zertifikatsprüfung und die dreifach abgesicherte Audiofreigabe (lokal vollständig/ACKed, Serverfreigabe, erneute Reconciliation) umgesetzt und am Gerät gelaufen. Alle vier priorisierten Teilpunkte sind seit 2026-09-12 umgesetzt und real am Gerät bestätigt: persistierte Retryklassen, ACK-/Schreibgrenzen-Stromausfalltests, automatische Credentialrotation und zuletzt die Verschlüsselung (eigene Einträge unten; deren erste zwei Geräteversuche schlugen an zwei echten, nur am Gerät sichtbaren Fehlern fehl — einer mbedtls-GCM-Pufferannahme und einem FAT-8.3-Dateinamensfehler —, der dritte Versuch lief sauber durch). Normative Grenze: `esp32-client/docs/BACKEND_REQUIREMENTS.md`. Offen bleibt nur noch der separat priorisierte spätere Systemaudit (Flash Encryption/Secure Boot, siehe unten).

- [ ] Implementiere feste, einzeln abschließbare Audiosegmente mit Sequenz, Zeitstempeln, Dauer, Größe und SHA-256 und teste Prozessabbruch, Display-aus-Simulation, Segmentgrenzen und beschädigte Dateien ohne echten Backendzugriff.
- [ ] Dokumentiere die Messergebnisse und verbleibenden Risiken des Audio-Prototyps in `android/docs/AUDIO_PIPELINE.md` und einem ADR und blockiere die vollständige Pipeline, falls Format, Foreground Service oder Recovery nicht reproduzierbar funktionieren.
- [ ] Implementiere eine SQLCipher-geschützte Room-Datenbank mit zufälligem installationsgebundenem 256-Bit-Schlüssel aus Android Keystore, Migrationstests und einer dokumentierten Strategie für Schlüsselverlust.
- [ ] Implementiere verschlüsselte lokale Audiodateien mit atomarem Schreiben, restriktiven Dateirechten, Integritätsprüfung, sicherer Bereinigung und Tests gegen Klartextreste, Teilwrites und beschädigte Ciphertexte.
- [ ] Implementiere die dauerhafte Segment- und Mutationsqueue mit idempotenten Schlüsseln, Abhängigkeiten, Prioritäten, Retry-Zuständen, Backoff und dem nach 24 Stunden sichtbaren Zustand `attention_required`.
- [ ] Implementiere WorkManager-Uploads mit Netzwerkbedingungen, begrenzter Parallelität, korrekten Multipart-Feldern, Hashprüfung, dauerhaftem ACK und Wiederanlauf nach Prozess- oder Gerätestart.
- [ ] Implementiere Reconciliation gegen den autoritativen Serverzustand für fehlende, bereits bestätigte, doppelte und widersprüchliche Segmente und teste alle Referenzfälle mit MockWebServer.
- [ ] Implementiere Stop, Drain, Finish und Abort als crash-sichere Zustandsautomaten, die keine bestätigten Daten erneut senden und unfertige lokale Arbeit nach Prozessverlust eindeutig wiederaufnehmen oder sichtbar blockieren.
- [ ] Implementiere die Compose-App-Shell mit Navigation, Material-Designsystem, klaren Lade-, Leer-, Offline-, Fehler- und Aufmerksamkeitszuständen sowie deutscher UI und englischem Ressourcenfallback.
- [ ] Implementiere Text- und Audio-Composer mit den vertraglich erlaubten Modi Memo, Query und Auto, wobei neue Notes per Text oder Memo entstehen und Bearbeitungen als neue serverseitig aufzulösende Erfassung statt als lokale direkte Revision gesendet werden.
- [ ] Implementiere den serververwalteten Chat mit lokaler Anzeige, Sendestatus, Wiederholung, Streamingereignissen und klarer Trennung zwischen Benutzertext, Memo, Serverantwort und fachlichen Entitäten.
- [ ] Implementiere den geschlossenen Server-Driven-UI-Renderer ausschließlich für den dokumentierten Komponentenkatalog und die erlaubten Aktionen und zeige unbekannte Komponenten oder Aktionen sicher und diagnostizierbar statt sie dynamisch auszuführen.
- [ ] Erstelle für jede erlaubte Dashboard-Komponente und Aktion Fixture-, Semantik-, Accessibility- und Roborazzi-Golden-Tests für Telefon, großen Bildschirm und relevante Fold-Breiten.
- [ ] Implementiere Dashboard-Snapshot, deterministische Reihenfolge, zehnstündige lokale Historie und SSE-Reconnect mit Cursor, Backoff, Lebenszyklusbezug und anschließendem autoritativem Snapshot-Abgleich.
- [ ] Implementiere den Offline-Knowledge-Snapshot mit Delta-Sync, Tombstones, Redirects, Cursorpersistenz und atomarer Anwendung, ohne Tasks oder Listen als App-Entitäten einzuführen.
- [ ] Implementiere durchsuchbare Notes und übrige freigegebene Entitäten über Room FTS4 mit normalisierten Treffern, Offlinefunktion, Redirectauflösung und Tests für Löschung, Umbenennung, Sonderzeichen und große Datenmengen.
- [ ] Implementiere adaptive Navigation und Layouts für kompakte, mittlere und erweiterte Fenster sowie Fold-/Unfold-Wechsel ohne Verlust von Eingaben, Aufnahmezustand, Navigation oder Scrollposition.
- [ ] Implementiere den offiziellen UnifiedPush-Connector mit Fake-Distributor für automatische Tests, inhaltsarmen verschlüsselten Wake-up-Signalen und anschließendem autoritativem Sync; dokumentiere den späteren echten ntfy-Gerätetest auf dem System des Auftraggebers.
- [ ] Implementiere vollständige Einstellungs-, Diagnose-, Queue-, Speicher-, Netzwerk-, Berechtigungs- und Datenschutzoberflächen mit exportierbaren technisch bereinigten Diagnosen ohne Tokens, Inhalte oder Schlüssel.
- [ ] Führe einen Sicherheits- und Datenschutzreview für Keystore, SQLCipher, Dateiverschlüsselung, Logs, Backups, Screenshots, Zwischenablage, TLS, Pending Intents, Exported Components und Release-Härtung durch und schließe alle kritischen Befunde mit Regressionstests.
- [ ] Führe einen Accessibility-Review mit TalkBack-Semantik, Fokusreihenfolge, Mindestgrößen, Kontrast, skalierter Schrift, reduzierter Bewegung und Bedienung aller Aufnahmezustände durch und dokumentiere automatisierte sowie manuelle Prüfschritte.
- [ ] Erzeuge einen gehärteten Release-Build ohne Debugflag, Cleartext, TLS-Bypass oder eingebettete Geheimnisse und unterstütze optionale lokale Signierung ausschließlich über Umgebungsvariablen oder gitignorierte `keystore.properties`. (Teilstand: Release-Build mit R8/Minify läuft grün im Gate; Signierungs- und Geheimnis-Checks noch offen.)
- [ ] Erstelle ein reproduzierbares Übergabepaket mit APK beziehungsweise AAB, SHA-256-Prüfsummen, Dependency- und Lizenzbericht, Testreport, bekannten Einschränkungen und exakten Installations- und Rückrollanweisungen für das System des Auftraggebers.
- [ ] Führe auf dem Build-PC das vollständige JVM-, Roborazzi-, MockWebServer-, Lint-, API-26- und API-36-Emulator-Gate aus und dokumentiere Toolversionen, Ergebnisse und ausgelassene reale Gerätetests ohne sie als bestanden auszugeben.
- [ ] Erstelle ein Runbook und Protokollvorlagen für die ausschließlich beim Auftraggeber auszuführenden Tests auf Pixel 9 Pro, Pixel Fold und optional OnePlus 9 Pro einschließlich echter Serverkommunikation, ntfy/UnifiedPush, Display aus, Prozessverlust, Netzwechsel und Vier-Stunden-Aufnahme.
- [ ] Führe auf dem System des Auftraggebers die reale Backend- und Geräteabnahme nach Runbook durch, protokolliere Android-Versionen und Umgebungsdaten und erfasse jeden Fehler reproduzierbar, ohne Ergebnisse vom Build-PC zu übertragen oder zu erfinden.
- [ ] Behebe Befunde aus der realen Abnahme in getrennten, regressionsgetesteten App-Tasks und wiederhole jeweils die betroffenen PC-, Emulator- und Gerätetests sowie die zugehörige Feature-Dokumentation.
- [ ] Führe zum Abschluss einen vollständigen Normrang-, Contract-, Link-, ADR-, Sicherheits-, Datenschutz-, Accessibility-, Lizenz- und Reproduzierbarkeitsaudit durch und liefere einen Abschlussbericht, der bestandene automatische Prüfungen klar von manuellen oder noch offenen Gerätetests trennt.

## Done

- [x] A01 geschlossen: `POST /api/client/v1/captures` legt für direkte Texteingaben eine text-only Client-/Ingestion-Session, genau einen idempotenten Chunk und per Repair einen Textjob an. Text und stabilisiertes Audiotranskript laufen danach durch dieselbe Segmentierungs-, Topic-, Artefakt-, Finalisierungs- und Promotionskette. `GET .../captures/{id}` spiegelt `processing`, `attention_required`, Recovery und `completed`; Query-Conversation/Turn entstehen erst nach dem fachlichen Abschluss. Der ESP bleibt unverändert reine Audioquelle. Der gezielte Paritäts-/Recoverytest und das vollständige M8-Gate einschließlich logischem Vier-Stunden-Soak sind grün. — Erledigt 2026-09-09.

- [x] Backend-Re-Audit gegen `BACKEND_LOGIK.md` (8. September 2026): A10 geschlossen, indem `worker.py all` nun neben Audio/Text/Artefakten auch Einzeltext-Captures und Chat-Turns verarbeitet. A09/W08 gehärtet: fachliche Finalisierung und Promotion laufen vor Client-`completed` und lokaler Audiofreigabe; jeder Processing-Jobzustand ungleich `done` blockiert, Promotionsfehler bleiben idempotent wiederaufnehmbar. Regression ergänzt für `parked`, Promotionsfehler, gesperrte Audiofreigabe und Recovery. Bekannten Shared-DB-Ausreißer in `m8_chat_push_contract_test.py` isoliert: fremde Dashboard-Karten mit vertragsgemäßem `entity_ref: null` werden bei der Suche nach der Test-Conversation sicher übersprungen. Alle D01–D22 sowie A01–A13/W01–W10 statisch neu geprüft; aktuelle Einordnung und der korrigierte Inventarstand von zwölf strukturierten LLM-Aufrufen in zehn Services stehen in `BACKEND_LOGIK.md`. Architekturfrage anschließend in AD-012 entschieden: kleine funktionierende `json_schema`-Schritte bleiben; freie Textausgabe bleibt eine beleg- und validatorpflichtige Taskausnahme statt eines globalen Fallbacks.

- [x] Ziehe alle umgebungsspezifischen Netzwerkeinstellungen aus `smart_notebook/config.py` in `.env`: `LLM_URL`, `EMBEDDING_URL`, `STT_URL`, `POSTGRES_HOST/PORT/DB/USER/PASSWORD` laufen jetzt über `os.getenv`, ohne Heimnetz-Adressen als Default. Fehlt eine der fünf umgebungsspezifischen Pflichtvariablen ohne sinnvollen Fallback (`LLM_URL`, `EMBEDDING_URL`, `STT_URL`, `POSTGRES_HOST`, `POSTGRES_PASSWORD`), bricht der Start mit `RuntimeError` und den fehlenden Namen ab, statt still gegen eine leere Adresse zu laufen. `smart_notebook/services/chat.py::get_applied_prompt()` hatte eine zweite, unabhängige Hartkodierung zum selben LLM-Host (`/apply-template`) — jetzt aus `LLM_URL` abgeleitet (`LLM_APPLY_TEMPLATE_URL`), keine zweite Variable. Neu: `.env.example` dokumentiert jede Variable aus `config.py` mit Zweck und unbedenklichem Beispielwert, einschließlich `CLIENT_DEVICE_AUTH_SECRET`, `CLIENT_OPERATOR_TOKEN`, `CLIENT_DEVICE_AUTH_REQUIRED`. `scripts/portability-check.py` hat zwei neue Regeln (`private-ip-address`, `internal-hostname`); sie überspringen `*.md`/`CHANGELOG*`-Dateien und den archivierten Codex-Handoff (`smart-notebook-codex-handoff-v1.0/`), da diese Heimnetz-Adressen absichtlich als historischen Kontext zitieren, und kennen eine feste Ausnahmeliste bekannter Nicht-Geheimnisse (ESP-AP-Default `192.168.4.1`, Android-Emulator-Alias `10.0.2.2`, Testfixtures). **Offen, bewusst nicht Teil dieser Änderung:** `POSTGRES_PASSWORD` trug den Wert `CHANGE_ME_TO_A_LONG_RANDOM_PASSWORD` fest verdrahtet in `config.py` — das war kein Platzhalter, sondern das tatsächlich aktive Postgres-Passwort, und `config.py` liegt bereits im gepushten Initial-Commit auf `github.com/hilarius-lab/stoa`. Auf Wunsch vorerst unverändert gelassen (Wert unverändert in die lokale, gitignorierte `.env` übernommen); Rotation auf dem Server steht weiter aus. `portability-check.py` findet daneben sechs vorbestehende, von dieser Änderung unabhängige `windows-path`-Befunde (`C:\Espressif\...` in `CLAUDE.md` und `esp32-client/docs/CLIENT_SERVER_STATE.md`, `C:/Windows`-Fallback in `esp32-client/tools/generate_font.py`) — nicht angefasst, da außerhalb dieses Auftrags. Verifiziert: `portability-check.py` (0 neue Befunde durch die neuen Regeln), `m8_esp_dashboard_projection_test.py`, `device_auth_test.py` und das volle `m8_release_gate_test.py` grün — alle drei App-Tests nur mit `CLIENT_DEVICE_AUTH_REQUIRED=false` im Prozess, weil die lokale `.env` produktionsscharf auf `true` steht (vorbestehend, nicht durch diese Änderung verursacht).

- [x] ESP32: letzte physische Sprachnotiz fuer Nutzerfeedback per lesendem USB-Export auswerten. Fuenf Segmente/42.56 s mit SHA-256 und Decode geprueft; deutsche Sprache mit lokalem STT erkannt. Gesprochene Positionsfehler des Displays in Korrektur uebernommen: Vollbildadressierung plus Teilrefresh-Wellenform statt versetztem Ausschnittspfad. Optische Abnahme durch Nutzer bestanden; Details in `esp32-client/CHANGELOG.md`.

- [x] ESP32: lokale Sprachnotiz (aktuell BOOT halten/loslassen), Bereitschafts-/Aufnahme-/Speicheranzeige und partielle E-Paper-Updates. Hardwareaufnahme mit AAC-LC mono 48 kHz / 64 kbit/s, zwei M4A-Segmente per USB mit SHA-256 verglichen und fehlerfrei mit FFmpeg decodiert. Nutzer bestaetigt sauberen Zaehler ohne Vollbildflackern; Startbild-Ghosting durch volle Auffrischung beim Eintritt in Bereitschaft behandelt. Details und Prototypgrenzen in `esp32-client/README.md` und `esp32-client/CHANGELOG.md`.

- [x] Führe vor jeder Änderung einen Repository- und Normrang-Audit anhand von `android/docs/APP_FUNCTIONAL_BOUNDARY.md`, `CLIENT_BACKEND_CONTRACT.md`, `android/docs/NATIVE_ANDROID_ARCHITECTURE.md`, `ARCHITECTURE_DECISIONS.md`, `AUDIO_ARCHITECTURE.md`, `android/docs/ANDROID_CLIENT_ROADMAP.md`, `ROADMAP.md` und `TESTING.md` durch und dokumentiere widersprüchliche oder fehlende Vorgaben ohne eigene Semantik in einem Readiness-Bericht. — Erledigt 2026-08-29: alle Normdokumente geprüft; Abweichungen (AGP-9-Built-in-Kotlin, `androidlib`-Convention, compileSdk 37.1, DataStore-Package-Naming) dokumentiert in `android/docs/adr/0001-project-scaffold-and-toolchain.md`.
- [x] Ermittle den tatsächlichen Stand aller `/api/client/*`-Operationen im FastAPI-Code, im OpenAPI-Snapshot, in den Referenzfixtures und in der Contract-Matrix und liefere eine maschinenlesbare Liste aller leeren oder unvollständigen Request- und Response-Schemas als Backend-Blocker. — Erledigt 2026-08-28/29: OpenAPI-Snapshot v1 (`contracts/client-openapi-v1.json`) hat 0 leere Schemas; M8-Gate `reports/m8-release-gate.json` `passed: true` (2026-08-28 14:01).
- [x] Ergänze als ausdrücklich getrennten Backend-Arbeitsblock vollständige Pydantic-Response-Modelle für sämtliche `/api/client/*`-Antworten, ohne bestehende Wire-Semantik zu verändern, und belege jedes Feld durch Implementierung, Vertrag und Tests. — Erledigt im Backend-Block; M8-Neulauf grün.
- [x] Implementiere als ausdrücklich getrennten Backend-Arbeitsblock `POST /api/client/v1/diagnostics/audio-upload-test` mit Prüfung von Multipart-Metadaten, MIME, Größe, SHA-256, M4A/AAC-LC, Samplerate, Kanälen, Bitrate, Dauer und produktiver FFmpeg-Decodierbarkeit, ohne Session, STT-Arbeit, Artefakte oder Evidence zu erzeugen und mit sicherer Löschung der Testbytes. — Erledigt; Endpoint in `CLIENT_CONTRACT_MATRIX.md` und OpenAPI-Snapshot vorhanden; M8-Diagnostic-Tests grün.
- [x] Ergänze die Capability-Antwort um den versionierten diagnostischen Audio-Upload-Vertrag und teste positive sowie negative Capability-, Format-, Größen-, Hash- und Decode-Fälle. — Erledigt; M8-Gate grün.
- [x] Erzeuge nach Abschluss des Backend-Arbeitsblocks `contracts/client-openapi-v1.json` reproduzierbar neu und lasse ein Gate fehlschlagen, sobald eine JSON-Antwort unter `/api/client/*` ein leeres oder nicht auflösbares Schema besitzt. — Erledigt; Snapshot v1 regeneriert, Schema-Gate Teil des M8-Gates.
- [x] Erzeuge beziehungsweise aktualisiere `contracts/client-reference-fixtures-v1.json` nach Abschluss des Backend-Arbeitsblocks und validiere jede Fixture automatisiert gegen das zugehörige OpenAPI-Schema. — Erledigt; Fixture-Datei existiert und wird im M8-Gate gegen das Schema validiert.
- [x] Führe das vollständige M8-Release-Gate nach den Backend-Vertragsänderungen aus und blockiere den Android-Start bei Fehlern, untypisierten Antworten, Fixture-Drift oder fachlichen Nebenwirkungen des diagnostischen Uploads. — Erledigt 2026-08-28: `reports/m8-release-gate.json` `passed: true`; Android-Start darauf basierend freigegeben.
- [x] Lege das native Android-Projekt ausschließlich unter `android/` an und stelle sicher, dass kein bestehender Backend-Code verschoben, dupliziert oder durch die App-Implementierung verändert wird. — Erledigt 2026-08-29: Scaffold mit 17 Modulen unter `android/`; `smart_notebook/` unverändert.
- [x] Erstelle unter `android/scripts` ein portables `bootstrap-check`-Skript, das Repository-Root, JDK 21, JVM-Target 17, `ANDROID_HOME`, SDK 36, Build-Tools, Platform-Tools und optional verfügbare API-26-/API-36-Systemimages prüft, ohne Benutzer-, Laufwerks- oder absolute Installationspfade festzuschreiben. — Erledigt 2026-08-29: `android/scripts/bootstrap-check.ps1`, 9/9 PASS (inkl. android-37.1-Platform-Check).
- [x] Erstelle unter `android/scripts` ein vollständiges `check-all`-Gate für Debug- und gehärtete Release-Builds, alle JVM-Tests, Lint, Contract-Tests, Roborazzi-Goldens und erforderliche Instrumentationstests. — Erledigt 2026-08-29 (Kern): `android/scripts/check-all.ps1` = bootstrap-check + docs-check + `:app:assembleDebug :app:assembleRelease test detekt ktlintCheck`; Lint, Contract-Tests, Roborazzi und Instrumentation werden in den jeweiligen Phasen in das Task-Set aufgenommen.
- [x] Erstelle unter `android/scripts` ein Dokumentations-Gate, das Pflichtdokumente, ADRs, interne Links, erwähnte Skripte, Modulnamen und Testkommandos auf Existenz und Konsistenz prüft. — Erledigt 2026-08-29: `android/scripts/docs-check.ps1` (Pflichtdokumente non-strict mit `-Strict`-Modus, ADR-Verzeichnis, relative Markdown-Links); PASSED.
- [x] Lege ein natives Gradle-Kotlin-DSL-Projekt mit Gradle Wrapper, `minSdk 26`, `compileSdk 36`, `targetSdk 36`, Gradle-JDK 21 und JVM-Target 17 an und verifiziere einen frischen Offline-wiederholbaren Debug-Build. — Erledigt 2026-08-29, dokumentierte Abweichung: compileSdk 37.1 statt 36 (AGP-9-Anforderung, ADR-0001); Wrapper, minSdk 26, targetSdk 36, JDK 21, JVM 17 wie vorgegeben; Debug-Build grün.
- [x] Richte einen Gradle-Version-Catalog, Convention Plugins, reproduzierbare Dependency-Locks und ausschließlich stabile Abhängigkeiten ohne Alpha-, Beta- oder RC-Versionen ein. — Erledigt 2026-08-29: `gradle/libs.versions.toml` (Single Source of Truth, nur Stable-Versionen), Convention Plugins (`android-application`, `androidlib`), `dependencyLocking { lockAllConfigurations() }` auf Root, 16 Modulen und `build-logic` (dort mit `ignoredDependencies` für `junit`/`hamcrest-core` wegen kotlin-dsl-Bug) — 19 Lockfiles; empirisch verifiziert: Katalogänderung lässt Build mit „not part of the dependency lock state“ fehlschlagen.
- [x] Generiere versionierte Request- und Response-Wire-DTOs ausschließlich aus dem freigegebenen OpenAPI-Snapshot und lasse den Build bei Snapshot- oder Generator-Drift sichtbar fehlschlagen. — Erledigt 2026-08-29: 72 generierte Typen aus `contracts/client-openapi-v1.json` (Generator `wiregen` in `build-logic`, byte-deterministisch, kommittiert unter `core:network/.../wire/generated/`); Drift-Gate `:core:network:verifyWireDtos` (byte-Vergleich) in `check` und `check-all.ps1`; `WireJson` + `UUID`/`Instant`-Contextual-Seriale; Contract-Tests `WireContractTest` (14 Tests); ADR-0002.
- [x] Implementiere explizite Mapper zwischen generierten Wire-DTOs und handgeschriebenen Domainmodellen und teste unbekannte Enumwerte, optionale Felder, fehlerhafte Payloads und Vorwärtskompatibilität ohne Wire-Typen in UI- oder Domainmodule durchsickern zu lassen. — Erledigt 2026-08-29: Domainmodelle + `WireResult`/`WireDecodeError` in `core:model` (ohne Wire-/Serialization-Abhängigkeiten); `WireDecoding` + 8 Mapper-Objects für 11 Domänen in `data/.../mapper/`; `MapperContractTest` (21 Tests: Fixture-Mapping, `UNKNOWN`-Fallbacks, optionale Felder, fehlerhafte Payloads, Vorwärtskompatibilität, Leak-Scan); Wire-Enums lenient (unbekannte Werte → `UNKNOWN`); `check-all.ps1` bestanden; ADR-0002 revidiert.
- [x] Implementiere einen strikt typisierten Fehler- und Ergebnisraum für Transport, Authentisierung, TLS, Kompatibilität, Validierung, Rate-Limit, Offlinezustand und Serverfehler und belege jede Abbildung durch Referenzfixtures. — Erledigt 2026-08-29: geschlossener `ClientErrorCode`-Katalog (33 stabile Codes nach `NATIVE_ANDROID_ARCHITECTURE.md` §13) + `ClientError` in `core:model` (ohne Wire-Abhängigkeiten); totaler, nie werfender `ClientErrorMapper` in `:data` (Wire-Umschlag → typisierter Code, `UNKNOWN_SERVER_ERROR`-Fallback mit geerbter `retry_class`, HTTP-Grundmapping bei fehlendem/unparsbarem Umschlag, mehrdeutige `409`/`410` ohne Semantik-Erfindung, TLS/Timeout/Netz-Transportfehler über Cause-Kette, `Retry-After` nur ganzzahlig und nur bei 429); `ClientErrorMappingTest` (12 Tests gegen Referenz-Fixture + schema-konforme Envelopes); `check-all.ps1` bestanden; ADR-0003.
- [x] Implementiere die sichere Konfigurationsspeicherung für Server-Schema, Host, Port, API-Basispfad und nicht geheime Präferenzen in Proto DataStore sowie Geheimnisse ausschließlich über Android Keystore. — Erledigt 2026-08-29 (dokumentierte Abweichung: `datastore-preferences` statt `datastore-proto`, ADR-0001/0004): `ServerProfile` + `ClientPreferences` (Defaults `de`/`android_aac_lc_v1`/10 s/kein Gerätename) in `core:model`; `AesGcmCipher` (AES-256-GCM, 12-Byte-Nonce, 128-Bit-Tag) + `AndroidKeyStoreKeyProtector` (nicht exportierbarer Schlüssel, Alias `com.smartnotebook.master.v1`, Material verlässt den Keystore nie) + totaler `SecretStore` (beschädigtes Blob → `null`) in `core:security`; `ConfigRepository` (Preferences-DataStore `smart_notebook_config`, minimale Validierung/Normalisierung, separates `smart_notebook_secrets` nur für Ciphertext) + Hilt `ConfigModule` in `:data`; 26 neue Unit-Tests (7 Cipher, 6 Store, 13 Repository); `check-all.ps1` bestanden; ADR-0004.
- [x] Implementiere den Setup-Wizard als wiederaufnehmbaren Zustandsautomaten für Willkommen, Serveradresse, TLS, Health, Capabilities, Kompatibilität, Installation, Authentisierung, diagnostischen Audio-Upload und Abschluss. — Erledigt 2026-08-30 (dokumentierte Abweichung: Probe-Implementierungen TLS/Health/Capabilities/Kompatibilität/Audio-Upload/Initial-Sync fehlen noch und folgen in den Folgeaufgaben; `WizardController`/`WizardViewModel` vorübergehend außerhalb des Hilt-Graphs, Einbindung in der App-Shell-Aufgabe): `WizardStep` (13 Schritte nach §7)/`WizardState`/`WizardEvent` + reine `WizardMachine` (Event-Guards, `profileConfirmed` bei `INSTALLATION`, GoBack → letzter sicherer Schritt, `ProbeBlocked` → `blocked`) in `core:model`; Probe-Ports (`ProbeOutcome`, 7 Marker-Interfaces, `WizardProbes`-Bundle); `WizardController` (Restore, automatische Probe bei Schrittwechsel, `retry`, `submitServerAddress` mit `CleartextNotAllowed`) + `WizardStateRepository`/`DataStoreWizardStateRepository` (`wizard.*`-Keys) in `:data`; `WizardViewModel` + Compose-UI in `feature:setup` (EN/DE); 59 neue Unit-Tests; `check-all.ps1` bestanden; ADR-0005.
- [x] Implementiere normalisierte URL-Zusammensetzung und Eingabevalidierung für Schema, Host, IPv4, IPv6, Port und API-Basispfad und teste ungültige, doppelte und nachgestellte Trennzeichen ohne produktiven Serverzugriff. — Erledigt 2026-08-29/30: `ServerAddressValidator` in `core:model` (Schema `http`/`https`; Host: striktes IPv4 ohne führende Nullen, durchgehende numerische Punktlabels als Fehl-IPv4 abgewehrt, IPv6 einfach/mit Kompression/mit Klammern — Speicherung mit Klammern für gültige `baseUrl`, RFC-1123-Hostnames; Port 1–65535, Schema-Defaults 80/443, keine führenden Nullen; Basispfad Default `/api`, doppelte/nachgestellte `/`, Whitespace, `?`, `#`, `\` abgelehnt); `ValidatedServerAddress.isCleartext` (Release-Blockade im Controller); 31 Unit-Tests inkl. doppelte und nachgestellte Trennzeichen, ohne Serverzugriff; Teil von ADR-0005.
- [x] Implementiere TLS-Vertrauensprüfung ohne unsicheren Bypass, klare Zertifikatsfehler und einen bewusst bestätigten Fingerprint-Wechsel, soweit dies der normative Vertrag zulässt, und dokumentiere das Bedrohungsmodell. — Erledigt 2026-08-30 (dokumentierte Abweichung: bewusst bestätigter Fingerprint-Wechsel wird NICHT implementiert, weil der normative Vertrag §6 „alle Zertifikate akzeptieren“-Optionen verbietet und Pinning nur zulässt, wenn der Serververtrag es ausdrücklich einführt — `CLIENT_BACKEND_CONTRACT.md` tut es nicht; ein bestätigbarer Fingerprint-Wechsel wäre ein nutzerbestätigbarer MITM-Bypass): `TlsProbe` in `core:network` (reiner TLS-Handshake ohne HTTP-Anfrage, nur Plattform-Trust, Endpoint-Identification `HTTPS` via `SSLParameters`, Profil-Timeouts, IPv6-Klammern); `TlsErrorClassifier` (klare Meldungen: abgelaufen, nicht gültig, Hostname-Abweichung, nicht vertraut, DNS, Refusal, Timeout); Cleartext als zweite Verteidigungsebene (`Blocked`/`Passed` je `allowCleartext`); 19 Unit-Tests (MockWebServer-Handshakes mit selbst signierter PKCS#12-Test-Fixture, Klassifizierung); Bedrohungsmodell in ADR-0006; `check-all.ps1` bestanden.
- [x] Implementiere Health-, Capability- und Versionskompatibilitätsprüfungen im Wizard gegen MockWebServer und Fixtures und blockiere inkompatible Server mit einer präzisen, nicht spekulativen Meldung. — Erledigt 2026-08-30: `WizardProbe.run` erhält `ClientPreferences`; `ProbeHttp` (OkHttp mit Profil-Timeouts, `Dispatchers.IO`, totaler Transport-/HTTP-Abgriff) in `:data`; `CapabilitiesProbe` prüft `GET client/health` (Status `ok`, HTTP-/Decodierfehler) und `GET client/capabilities` (Status `ready`/`degraded` fortsetzen, `maintenance` blockieren, Featureflags `rest`, `sse`, `audio_upload`, `audio_upload_diagnostics`, gewähltes Audioprofil und positive `request_timeout_seconds`); `CompatibilityProbe` ruft `GET client/v1/contract` mit `x-smart-notebook-contract: 1` auf und blockiert `API_INCOMPATIBLE` bzw. `contract.supported` ohne App-Version mit exakter Supported-Liste; 24 neue MockWebServer/Fixture-Tests in `:data`; ADR-0007.
- [x] Aktualisiere nach dem Backend-Arbeitsblock `CLIENT_BACKEND_CONTRACT.md`, `CLIENT_CONTRACT_MATRIX.md`, `android/docs/ANDROID_CLIENT_ROADMAP.md`, `ROADMAP.md` und die M8-Gates konsistent und dokumentiere den diagnostischen Endpoint ausdrücklich als nebenwirkungsfrei. — Erledigt 2026-08-30: Endpoint `POST /api/client/v1/diagnostics/audio-upload-test` in `CLIENT_BACKEND_CONTRACT.md`, `ROADMAP.md`, `ANDROID_CLIENT_ROADMAP.md` und `ANDROID_APP_IMPLEMENTATION_PROMPT.md` ausdrücklich dokumentiert; `CLIENT_CONTRACT_MATRIX.md` war bereits konsistent; `m8_release_gate_test.py` prüft jetzt Endpoint und Nebenwirkungsfreiheit in ROADMAP-M8, Backend-Vertrag, Contract-Matrix und Android-Roadmap; Docs-Check, M8-Release-Docs-Assertion und M8-Client-Schema-Gate grün.
- [x] Repariere die Graphify-Integration portabel, indem der Repository-Root dynamisch ermittelt, `.work/graphify-out/` als Ausgabe verwendet, der Toolpfad nur aus Umgebungsvariable oder gitignorierter lokaler Konfiguration gelesen und ein fehlendes Tool verständlich als optional gemeldet wird. — Erledigt 2026-08-30: `scripts/graphify.py` ermittelt Repository-Root dynamisch, setzt `GRAPHIFY_OUT` auf `.work/graphify-out/`, liest `GRAPHIFY_TOOL` aus Umgebungsvariable oder `scripts/graphify.local.env` (beide gitignored/Example ohne konkrete Pfad), meldet fehlendes Tool optional mit Exit 0 und übergibt Exit-Codes; manuelle Prüfläufe für Optional-, Config- und Fehlerpfad grün.
- [x] Ergänze ein Portabilitäts-Gate, das eingecheckte Verweise auf fremde Repositories, konkrete Benutzerprofile, lokale Laufwerke, Keystores, Passwörter und maschinenspezifische Android- oder JDK-Pfade verhindert. — Erledigt 2026-08-30: `scripts/portability-check.py` prüft Windows-/Unix-Pfade, Benutzerprofile, fremde Repo-Marker, absolute Keystore-Pfade und konkrete Credentials, skippt generierte/lokale/binary Dateien und ist als Schritt 3/4 in `android/scripts/check-all.ps1` integriert; `.gitignore` schließt `*.local.env`, `android/local.properties`, `android/keystore.properties` und Android-Build-Verzeichnisse aus; historische Pfade in `ANDROID_PLANNING_QUESTIONS.md` durch Platzhalter ersetzt; `check-all.ps1 -Tasks help` und Portabilitäts-Prüflauf mit positivem und negativem Testfall grün.
- [x] Erstelle unter `android/scripts` ein diff-skaliertes `check-changed`-Gate mit gemeinsamen Gradle-Downloads und isolierten Build-Ausgaben, das alle von einer Änderung betroffenen Module kompiliert, lintet und testet. — Erledigt 2026-08-30: `android/scripts/check-changed.ps1` unterstützt Git-Diff, explizite `-Paths` und `-Modules`, berechnet transitive Abhängige aus den Modul-Build-Dateien, führt `assembleDebug`, `test`, `detekt` und `ktlintCheck` für betroffene Module aus und verwendet `--project-cache-dir .work/check-changed` bei gemeinsamem Gradle-Download-Cache; ohne Git fällt es auf das vollständige Modul-Set zurück; DryRun- und gezielter Gradle-Prüflauf für `core:security` grün.
- [x] Fülle die lokalen Work-Loop-Anweisungen für Check, Review, Konventionen und Projektregeln mit den realen Android-Kommandos und dem Normrang und mache klar, dass App-Agenten weder Backendsemantik erfinden noch Backendcode ändern dürfen. — Erledigt 2026-08-30: `CLAUDE.md` mit Normrang, unveränderlichen Grenzen, Arbeitsweise und Check-Kommandos; `.claude/commands/check.md` für diff-skalierten/vollständigen Check plus Portabilität; `.claude/commands/review.md` als Review-Checkliste für Vertrag, Architektur, funktionale Grenze, Audio, Privatsphäre, Barrierefreiheit und Verifikation; `.work/conventions.md` mit lokalem Project-Integration-Bereich (Repository-Layout, Toolchain, Fresh-Worktree-Setup, Gates, Excluded-from-commits, Agentengrenzen); `check-all.ps1 -Tasks help` und Portabilitäts-Check grün.
- [x] Implementiere die in `android/docs/NATIVE_ANDROID_ARCHITECTURE.md` festgelegte Multi-Modul-Struktur mit gerichteten Abhängigkeiten und automatischem Architekturtest gegen verbotene Modulzugriffe. (Teilstand: 17 Module mit gerichtetem Abhängigkeitsgraph existieren; Architekturtest fehlt noch.) — Erledigt 2026-08-31: `architectureTest` als Configuration-Cache-kompatibler Root-Gradle-Task über `build-logic/.../architecture-check`; prüft 16 Blattmodule, erlaubten Modulgraph, Zyklen, externe Produktionsabhängigkeiten und verbotene direkte Netzwerk-/Persistenz-/UI-/Dateizugriffe; `check-all.ps1` und `check-changed.ps1` integrieren den Task; ADR-0008, CHANGELOG, README und Task-Queue aktualisiert; aktueller Graph und gezielte Negativtests verifiziert.
- [x] Richte Kotlin- und Compose-Formatierung, Android Lint, statische Analyse und verbindliche Compilerwarnungen ein und integriere sie in die lokalen Gates. — Erledigt 2026-08-31: Kotlin `allWarningsAsErrors` und Java `-Werror` für App-Module sowie `build-logic`; striktes AGP-Lint-Profil (`abortOnError`, `checkReleaseBuilds`, `checkTestSources`, `warningsAsErrors`, `checkDependencies = false`) in `android-application` und `androidlib`; `lint` in `check-all.ps1` und `:module:lint` in `check-changed.ps1`; Debug-Cleartext nur für `localhost`, `127.0.0.1` und `10.0.2.2`; Backup-Deaktivierung über `dataExtractionRules`/`fullBackupContent`; Launcher-Icon als Vektor-Ressource; Lint-, `check-all.ps1`, Docs- und Portabilitätscheck grün; ADR-0009, CHANGELOG, README und Work-Loop-Doku aktualisiert.
- [x] Konfiguriere englische Fallback-Ressourcen unter `values/`, deutsche Ressourcen unter `values-de/` und ein Gate gegen hartcodierte sichtbare UI-Texte. — Erledigt 2026-08-31: `uiTextCheck` als Root-Gradle-Task prüft hartcodierte sichtbare UI-Texte in Kotlin/Java und XML sowie Parität zwischen `values/` und `values-de/`; Kommentare und String-Inhalte werden vor der Prüfung entfernt, eng umrissene Ausnahmen über `ui-text:allow`; Setup-UI verwendet resourcebasierte `https`/`http`-Labels; `uiTextCheck`, Build-Logic-Tests, gezieltes `check-changed`-Gate und Negativtest grün; ADR-0010, CHANGELOG, README, `NATIVE_ANDROID_ARCHITECTURE.md` §15 und Task-Queue aktualisiert.
- [x] Konfiguriere Roborazzi und Robolectric für deterministische JVM-Screenshottests, dokumentiere das Golden-Update-Verfahren und verhindere unbeabsichtigte Golden-Aktualisierungen im normalen Prüflauf. — Erledigt 2026-08-31: Roborazzi `1.73.0` und Robolectric `4.16.1` für `:core:designsystem` und `:core:serverui` konfiguriert, Golden-Output unter `src/screenshots/`, feste Robolectric-Konfiguration mit SDK 36, `GraphicsMode.NATIVE` und expliziten Qualifiern; globale `gradle.properties` stellt `record=false`, `compare=false` und `verify=true` sicher, dadurch verifizieren normale `test`-, `check-all`- und `check-changed`-Läufe Goldens und aktualisieren sie nicht; `recordRoborazziDebug` ist ausschließlich das explizite Golden-Update-Verfahren; erster Designsystem-Golden-Test für `SmartNotebookTheme` und `SmartNotebookCard`; `android/docs/TESTING.md`, ADR-0011, CHANGELOG, README, `NATIVE_ANDROID_ARCHITECTURE.md` §15, `CLAUDE.md`, `.claude/commands/check.md` und `.work/conventions.md` aktualisiert; Roborazzi-Verify, Modul-Checks, `check-all.ps1`, Docs-Check und Portabilitätscheck grün.
- [x] Konfiguriere API-26- und API-36-AVDs mit `google_apis` und x86_64 über portable Skripte, wobei ein fehlender Emulator PC-basierte JVM-Prüfungen nicht verdeckt und Instrumentation klar als separates Gate ausweist. — Erledigt 2026-08-31: portables AVD-Skriptset unter `android/scripts/avd/` (`avd-setup.ps1`, `avd-status.ps1`, `avd-install.ps1`, `avd-launch.ps1`, `avd-instrumentation.ps1`); `SmartNotebookApi26` und `SmartNotebookApi36` mit `system-images;android-26;google_apis;x86_64` bzw. `system-images;android-36;google_apis;x86_64` angelegt; `bootstrap-check.ps1` meldet `platform-tools`, Emulator, Systemimages und AVDs nur als optionale Warnungen und blockiert JVM-Prüfungen nicht; `avd-instrumentation.ps1` ist das separate Emulator-Gate und wird nicht von `check-all.ps1` oder `check-changed.ps1` angestoßen; Smoke-Tests für API 26 und API 36 mit Debug-APK-Installation, Launcher-Start und Emulator-Stop grün; ADR-0012, CHANGELOG, README, `android/docs/TESTING.md`, `NATIVE_ANDROID_ARCHITECTURE.md` §15, `CLAUDE.md`, `.claude/commands/check.md` und `.work/conventions.md` aktualisiert.
- [x] Implementiere den Wizard-Schritt für den diagnostischen Audio-Upload ausschließlich gegen den freigegebenen nebenwirkungsfreien Endpoint und prüfe ACK, Hash, Formatmetadaten, Timeout, Wiederholung und garantierte Trennung von der fachlichen Memo-Pipeline. — Erledigt 2026-08-31 (dokumentierter Teilstand: eingebettete synthetische M4A/AAC-LC-Prüfdatei über `EmbeddedDiagnosticAudioSource`; native `AudioRecord`/`MediaCodec`/`MediaMuxer`-Quelle folgt als separate Audio-Risikoprototyp-Aufgabe): `AudioDiagnosticProbe` in `:data` ruft `POST /api/client/v1/diagnostics/audio-upload-test` über `ProbeHttp.postMultipart` auf; ACK = HTTP 200 plus vertragskonformer `AudioDiagnosticResponse`; SHA-256 über exakt hochgeladene Bytes vor dem Upload; Validierung von Kompatibilität, `durable_state_created = false`, Profil, MIME, Container, Codec, Samplerate, Kanälen, Bitrate, Dauer und Byte-Länge; `InterruptedIOException` wird als `NETWORK_TIMEOUT` klassifiziert; MockWebServer-Tests für Request-Form, HTTP-Fehler, Decode-Fehler, Transportfehler, Timeout, Wiederholung, Hash-Mismatch und Metadatenabweichungen; keine Session, STT, dauerhaften Audio-Bytes, Queue, WorkManager- oder Room-Schreibung; `:data:test` und `check-changed.ps1 -Paths "data\src"` grün; ADR-0013, CHANGELOG, Roadmap und Testing-Doku aktualisiert.
- [x] Erstelle vor der vollständigen Oberfläche einen ausführbaren Audio-Risikoprototyp mit `AudioRecord`, `MediaCodec` und `MediaMuxer`, der M4A/AAC-LC in den vertraglich festgelegten Parametern erzeugt und sich auf API 26 sowie API 36 instrumentiert prüfen lässt. — Erledigt 2026-09-01 (dokumentierter Teilstand: Prototyp ohne `RecordingForegroundService`, Segmentrotation, Pause/Resume, Recovery, Dateiverschlüsselung, Queue oder Upload): `AacLcM4aRecorder` in `:service:recording` erfasst PCM mit `VOICE_RECOGNITION` und `MIC`-Fallback, kodiert AAC-LC mit `MediaCodec` und `CONFIGURE_FLAG_ENCODE`, muxt M4A mit `MediaMuxer` und validiert MIME, Samplerate, Kanalzahl, AAC-LC, Bitrate, Dauer und SHA-256 über `M4aSegmentValidator`; messtechnische Dauer-Kompensation für gerätespezifische Encoder-Delay-/Padding-Abweichungen innerhalb der AAC-Frame-Toleranz; `avd-instrumentation.ps1` unterstützt `-PreInstallTask`, `-PreInstallApkDirectory` und `-GrantAllPermissions`; `check-changed.ps1 -Modules ":service:recording"` und `avd-instrumentation.ps1` für API 26/36 grün; ADR-0014, ADR-0012, CHANGELOG, Roadmap und Testing-Doku aktualisiert.
- [x] Implementiere für den Audio-Risikoprototyp einen echten `RecordingForegroundService` mit korrektem Service-Typ, persistenter Benachrichtigung, Berechtigungsfluss, Pause, Resume und Stop und teste Zustandsübergänge sowie verweigerte Berechtigungen. — Erledigt 2026-09-01 (dokumentierter Teilstand: Foreground-Service ohne Notification-Aktionen, feste Segmentrotation, Recovery, Dateiverschlüsselung, Queue oder Upload): `RecordingForegroundService` in `:service:recording` ist nicht exportiert, verwendet `foregroundServiceType="microphone"`, `START_NOT_STICKY` und ab API 30 den microphone-Typ bei `startForeground`; `RecordingSessionController` besitzt den geschlossenen Zustandsraum `Idle`/`Preparing`/`Recording`/`Paused`/`Stopping`/`PermissionRequired`/`Failed`; `AndroidRecordingPermissionChecker` prüft `RECORD_AUDIO` und ab API 33 `POST_NOTIFICATIONS`; persistente Low-Importance-Service-Benachrichtigung ohne Aktionen; `NativeAacLcRecordingEngine` pausiert durch Segmentabschluss und erzeugt beim Resume ein neues M4A-Segment; `RecordingControllerRegistry` ermöglicht testbaren Zugriff ohne UI; `RecordingSessionControllerTest` prüft Zustandsübergänge und verweigerte Berechtigungen auf JVM-Ebene; `RecordingForegroundServiceInstrumentedTest` prüft Start, aktive Benachrichtigung, Pause, Resume, Stop und zwei validierte M4A-Segmente auf API 26/36; `check-changed.ps1 -Modules ":service:recording"` und `avd-instrumentation.ps1` für API 26/36 grün; ADR-0015, ADR-0012, CHANGELOG, Roadmap, `NATIVE_ANDROID_ARCHITECTURE.md` und Testing-Doku aktualisiert.
